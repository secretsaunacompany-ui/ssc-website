node_modules/
# package-lock.json is deliberately TRACKED (P-C): the deploy story rests on
# reproducible builds, so the lockfile is committed, not ignored.
.netlify/
.env
.env.*
.vscode/
.idea/
.DS_Store
*.log
dist/
.cache/
_site/

# Visual diff harness working directory (builds, screenshots, asset cache)
.visual-diff/
