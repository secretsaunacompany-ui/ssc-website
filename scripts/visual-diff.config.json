{
  "widths": [
    1440,
    390
  ],
  "maxLayoutShiftPx": 4,
  "minShiftCoverage": 0.95,
  "maxChangedPct": 0.5,
  "maxOffsetDivergencePx": 400,
  "maxScaleDivergence": 0.05,
  "minFitConfidence": 0.75,
  "expectedToChange": [
    {
      "route": "/squamish/",
      "expires": "2026-10-29",
      "reason": "SUB-PAGE HEROES, 2026-09-06, measured ce7732a..84d9b3c. The page opened on a bare text band -- a `.page-hero` with `padding: 12rem 5% 6rem` and nothing in it but the h1 and one line -- and now opens on a 60svh photograph with that same copy on a bottom scrim at the gutter. Doc 10 s2.3 specified this component and s5.3 named it; the 2026-09-03 code-refresh executed the section's two DELETION corollaries and added nothing, which is why ten pages went blank. THE COPY IS UNCHANGED: every h1 and subtitle string is byte-identical, certified by the dom-integrity run at ce7732a..a850d11 (36/36 fingerprints identical modulo 22 measured declarations, PASS). changedPct and heightDelta are waived on that certificate: the pixels moved because a photograph arrived where there was black, and the height moved because the box is taller than the band it replaces. WHAT IS NOT WAIVED, AND MUST NOT BE. layoutShiftMaxPx and shiftCoverage stay RED and are REFUSED RESIDUALS in the form _notes.residualsNotDeclared requires, not waivers and not overrides. They are red for the reason the change exists: everything below the hero moves down by the difference between the old band and the new box, which is REAL, INTENDED layout movement, and an instrument reporting zero for it would be broken. The measured shift tracks the height delta almost exactly on every route, which is the evidence that the movement is a rigid translation of the page below the hero and not content reflowing. Coverage falls for the known row-matcher reason witnessed four times in the retired entries: the matcher has no per-pixel tolerance and cannot re-find rows whose luminance a photograph and a gradient changed where a flat black band used to be. RETIREMENT: this harness's baseline IS main, so the merge is the baseline advance and these residuals retire on the first run after the push. MEASURED, not predicted. /squamish/@1440: changedPct 18.940% (waived), heightDelta +127px (waived), offset +128px, scale x1.000, layoutShiftMaxPx 130 (REFUSED), shiftCoverage 0.821 (REFUSED). /squamish/@390: changedPct 22.230% (waived), heightDelta +124px (waived), layoutShiftMaxPx 124 (REFUSED), shiftCoverage within budget.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/sea-to-sky/",
      "expires": "2026-10-29",
      "reason": "SUB-PAGE HEROES, 2026-09-06, measured ce7732a..84d9b3c. The page opened on a bare text band -- a `.page-hero` with `padding: 12rem 5% 6rem` and nothing in it but the h1 and one line -- and now opens on a 60svh photograph with that same copy on a bottom scrim at the gutter. Doc 10 s2.3 specified this component and s5.3 named it; the 2026-09-03 code-refresh executed the section's two DELETION corollaries and added nothing, which is why ten pages went blank. THE COPY IS UNCHANGED: every h1 and subtitle string is byte-identical, certified by the dom-integrity run at ce7732a..a850d11 (36/36 fingerprints identical modulo 22 measured declarations, PASS). changedPct and heightDelta are waived on that certificate: the pixels moved because a photograph arrived where there was black, and the height moved because the box is taller than the band it replaces. WHAT IS NOT WAIVED, AND MUST NOT BE. layoutShiftMaxPx and shiftCoverage stay RED and are REFUSED RESIDUALS in the form _notes.residualsNotDeclared requires, not waivers and not overrides. They are red for the reason the change exists: everything below the hero moves down by the difference between the old band and the new box, which is REAL, INTENDED layout movement, and an instrument reporting zero for it would be broken. The measured shift tracks the height delta almost exactly on every route, which is the evidence that the movement is a rigid translation of the page below the hero and not content reflowing. Coverage falls for the known row-matcher reason witnessed four times in the retired entries: the matcher has no per-pixel tolerance and cannot re-find rows whose luminance a photograph and a gradient changed where a flat black band used to be. RETIREMENT: this harness's baseline IS main, so the merge is the baseline advance and these residuals retire on the first run after the push. MEASURED, not predicted. /sea-to-sky/@1440: changedPct 20.312% (waived), heightDelta +127px (waived), offset +128px, scale x1.000, layoutShiftMaxPx 128 (REFUSED), shiftCoverage 0.798 (REFUSED). /sea-to-sky/@390: changedPct 25.061% (waived), heightDelta +124px (waived), layoutShiftMaxPx 124 (REFUSED), shiftCoverage within budget.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/about/",
      "expires": "2026-10-29",
      "reason": "SUB-PAGE HEROES, 2026-09-06, measured ce7732a..84d9b3c. The page opened on a bare text band -- a `.page-hero` with `padding: 12rem 5% 6rem` and nothing in it but the h1 and one line -- and now opens on a 60svh photograph with that same copy on a bottom scrim at the gutter. Doc 10 s2.3 specified this component and s5.3 named it; the 2026-09-03 code-refresh executed the section's two DELETION corollaries and added nothing, which is why ten pages went blank. THE COPY IS UNCHANGED: every h1 and subtitle string is byte-identical, certified by the dom-integrity run at ce7732a..a850d11 (36/36 fingerprints identical modulo 22 measured declarations, PASS). changedPct and heightDelta are waived on that certificate: the pixels moved because a photograph arrived where there was black, and the height moved because the box is taller than the band it replaces. WHAT IS NOT WAIVED, AND MUST NOT BE. layoutShiftMaxPx and shiftCoverage stay RED and are REFUSED RESIDUALS in the form _notes.residualsNotDeclared requires, not waivers and not overrides. They are red for the reason the change exists: everything below the hero moves down by the difference between the old band and the new box, which is REAL, INTENDED layout movement, and an instrument reporting zero for it would be broken. The measured shift tracks the height delta almost exactly on every route, which is the evidence that the movement is a rigid translation of the page below the hero and not content reflowing. Coverage falls for the known row-matcher reason witnessed four times in the retired entries: the matcher has no per-pixel tolerance and cannot re-find rows whose luminance a photograph and a gradient changed where a flat black band used to be. RETIREMENT: this harness's baseline IS main, so the merge is the baseline advance and these residuals retire on the first run after the push. MEASURED, not predicted. /about/@1440: changedPct 25.767% (waived), heightDelta +126px (waived), offset +127px, scale x1.000, layoutShiftMaxPx 241 (REFUSED), shiftCoverage 0.908 (REFUSED). /about/@390: changedPct 31.838% (waived), heightDelta +124px (waived), layoutShiftMaxPx 124 (REFUSED), shiftCoverage 0.919 (REFUSED).",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/saunas/",
      "expires": "2026-10-29",
      "reason": "SUB-PAGE HEROES, 2026-09-06, measured ce7732a..84d9b3c. The page opened on a bare text band -- a `.page-hero` with `padding: 12rem 5% 6rem` and nothing in it but the h1 and one line -- and now opens on a 60svh photograph with that same copy on a bottom scrim at the gutter. Doc 10 s2.3 specified this component and s5.3 named it; the 2026-09-03 code-refresh executed the section's two DELETION corollaries and added nothing, which is why ten pages went blank. THE COPY IS UNCHANGED: every h1 and subtitle string is byte-identical, certified by the dom-integrity run at ce7732a..a850d11 (36/36 fingerprints identical modulo 22 measured declarations, PASS). changedPct and heightDelta are waived on that certificate: the pixels moved because a photograph arrived where there was black, and the height moved because the box is taller than the band it replaces. WHAT IS NOT WAIVED, AND MUST NOT BE. layoutShiftMaxPx and shiftCoverage stay RED and are REFUSED RESIDUALS in the form _notes.residualsNotDeclared requires, not waivers and not overrides. They are red for the reason the change exists: everything below the hero moves down by the difference between the old band and the new box, which is REAL, INTENDED layout movement, and an instrument reporting zero for it would be broken. The measured shift tracks the height delta almost exactly on every route, which is the evidence that the movement is a rigid translation of the page below the hero and not content reflowing. Coverage falls for the known row-matcher reason witnessed four times in the retired entries: the matcher has no per-pixel tolerance and cannot re-find rows whose luminance a photograph and a gradient changed where a flat black band used to be. RETIREMENT: this harness's baseline IS main, so the merge is the baseline advance and these residuals retire on the first run after the push. MEASURED, not predicted. /saunas/@1440: changedPct 35.100% (waived), heightDelta +127px (waived), offset +128px, scale x1.000, layoutShiftMaxPx 129 (REFUSED), shiftCoverage 0.507 (REFUSED). /saunas/@390: changedPct 41.912% (waived), heightDelta +124px (waived), layoutShiftMaxPx 125 (REFUSED), shiftCoverage 0.879 (REFUSED).",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/whistler/",
      "expires": "2026-10-29",
      "reason": "SUB-PAGE HEROES, 2026-09-06, measured ce7732a..84d9b3c. The page opened on a bare text band -- a `.page-hero` with `padding: 12rem 5% 6rem` and nothing in it but the h1 and one line -- and now opens on a 60svh photograph with that same copy on a bottom scrim at the gutter. Doc 10 s2.3 specified this component and s5.3 named it; the 2026-09-03 code-refresh executed the section's two DELETION corollaries and added nothing, which is why ten pages went blank. THE COPY IS UNCHANGED: every h1 and subtitle string is byte-identical, certified by the dom-integrity run at ce7732a..a850d11 (36/36 fingerprints identical modulo 22 measured declarations, PASS). changedPct and heightDelta are waived on that certificate: the pixels moved because a photograph arrived where there was black, and the height moved because the box is taller than the band it replaces. WHAT IS NOT WAIVED, AND MUST NOT BE. layoutShiftMaxPx and shiftCoverage stay RED and are REFUSED RESIDUALS in the form _notes.residualsNotDeclared requires, not waivers and not overrides. They are red for the reason the change exists: everything below the hero moves down by the difference between the old band and the new box, which is REAL, INTENDED layout movement, and an instrument reporting zero for it would be broken. The measured shift tracks the height delta almost exactly on every route, which is the evidence that the movement is a rigid translation of the page below the hero and not content reflowing. Coverage falls for the known row-matcher reason witnessed four times in the retired entries: the matcher has no per-pixel tolerance and cannot re-find rows whose luminance a photograph and a gradient changed where a flat black band used to be. RETIREMENT: this harness's baseline IS main, so the merge is the baseline advance and these residuals retire on the first run after the push. MEASURED, not predicted. /whistler/@1440: changedPct 19.921% (waived), heightDelta +126px (waived), offset +127px, scale x1.000, layoutShiftMaxPx 128 (REFUSED), shiftCoverage 0.881 (REFUSED). /whistler/@390: changedPct 23.439% (waived), heightDelta +124px (waived), layoutShiftMaxPx 171 (REFUSED), shiftCoverage within budget.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/vancouver/",
      "expires": "2026-10-29",
      "reason": "SUB-PAGE HEROES, 2026-09-06, measured ce7732a..84d9b3c. The page opened on a bare text band -- a `.page-hero` with `padding: 12rem 5% 6rem` and nothing in it but the h1 and one line -- and now opens on a 60svh photograph with that same copy on a bottom scrim at the gutter. Doc 10 s2.3 specified this component and s5.3 named it; the 2026-09-03 code-refresh executed the section's two DELETION corollaries and added nothing, which is why ten pages went blank. THE COPY IS UNCHANGED: every h1 and subtitle string is byte-identical, certified by the dom-integrity run at ce7732a..a850d11 (36/36 fingerprints identical modulo 22 measured declarations, PASS). changedPct and heightDelta are waived on that certificate: the pixels moved because a photograph arrived where there was black, and the height moved because the box is taller than the band it replaces. WHAT IS NOT WAIVED, AND MUST NOT BE. layoutShiftMaxPx and shiftCoverage stay RED and are REFUSED RESIDUALS in the form _notes.residualsNotDeclared requires, not waivers and not overrides. They are red for the reason the change exists: everything below the hero moves down by the difference between the old band and the new box, which is REAL, INTENDED layout movement, and an instrument reporting zero for it would be broken. The measured shift tracks the height delta almost exactly on every route, which is the evidence that the movement is a rigid translation of the page below the hero and not content reflowing. Coverage falls for the known row-matcher reason witnessed four times in the retired entries: the matcher has no per-pixel tolerance and cannot re-find rows whose luminance a photograph and a gradient changed where a flat black band used to be. RETIREMENT: this harness's baseline IS main, so the merge is the baseline advance and these residuals retire on the first run after the push. MEASURED, not predicted. /vancouver/@1440: changedPct 19.130% (waived), heightDelta +127px (waived), offset +128px, scale x1.000, layoutShiftMaxPx 240 (REFUSED), shiftCoverage 0.812 (REFUSED). /vancouver/@390: changedPct 21.988% (waived), heightDelta +77px (waived), layoutShiftMaxPx 77 (REFUSED), shiftCoverage 0.945 (REFUSED).",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/north-shore/",
      "expires": "2026-10-29",
      "reason": "SUB-PAGE HEROES, 2026-09-06, measured ce7732a..84d9b3c. The page opened on a bare text band -- a `.page-hero` with `padding: 12rem 5% 6rem` and nothing in it but the h1 and one line -- and now opens on a 60svh photograph with that same copy on a bottom scrim at the gutter. Doc 10 s2.3 specified this component and s5.3 named it; the 2026-09-03 code-refresh executed the section's two DELETION corollaries and added nothing, which is why ten pages went blank. THE COPY IS UNCHANGED: every h1 and subtitle string is byte-identical, certified by the dom-integrity run at ce7732a..a850d11 (36/36 fingerprints identical modulo 22 measured declarations, PASS). changedPct and heightDelta are waived on that certificate: the pixels moved because a photograph arrived where there was black, and the height moved because the box is taller than the band it replaces. WHAT IS NOT WAIVED, AND MUST NOT BE. layoutShiftMaxPx and shiftCoverage stay RED and are REFUSED RESIDUALS in the form _notes.residualsNotDeclared requires, not waivers and not overrides. They are red for the reason the change exists: everything below the hero moves down by the difference between the old band and the new box, which is REAL, INTENDED layout movement, and an instrument reporting zero for it would be broken. The measured shift tracks the height delta almost exactly on every route, which is the evidence that the movement is a rigid translation of the page below the hero and not content reflowing. Coverage falls for the known row-matcher reason witnessed four times in the retired entries: the matcher has no per-pixel tolerance and cannot re-find rows whose luminance a photograph and a gradient changed where a flat black band used to be. RETIREMENT: this harness's baseline IS main, so the merge is the baseline advance and these residuals retire on the first run after the push. MEASURED, not predicted. /north-shore/@1440: changedPct 17.021% (waived), heightDelta +127px (waived), offset +128px, scale x1.000, layoutShiftMaxPx 128 (REFUSED), shiftCoverage 0.811 (REFUSED). /north-shore/@390: changedPct 20.255% (waived), heightDelta +76px (waived), layoutShiftMaxPx 77 (REFUSED), shiftCoverage 0.944 (REFUSED).",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/gallery/",
      "expires": "2026-10-29",
      "reason": "SUB-PAGE HEROES, 2026-09-06, measured ce7732a..84d9b3c. The page opened on a bare text band -- a `.page-hero` with `padding: 12rem 5% 6rem` and nothing in it but the h1 and one line -- and now opens on a 60svh photograph with that same copy on a bottom scrim at the gutter. Doc 10 s2.3 specified this component and s5.3 named it; the 2026-09-03 code-refresh executed the section's two DELETION corollaries and added nothing, which is why ten pages went blank. THE COPY IS UNCHANGED: every h1 and subtitle string is byte-identical, certified by the dom-integrity run at ce7732a..a850d11 (36/36 fingerprints identical modulo 22 measured declarations, PASS). changedPct and heightDelta are waived on that certificate: the pixels moved because a photograph arrived where there was black, and the height moved because the box is taller than the band it replaces. WHAT IS NOT WAIVED, AND MUST NOT BE. layoutShiftMaxPx and shiftCoverage stay RED and are REFUSED RESIDUALS in the form _notes.residualsNotDeclared requires, not waivers and not overrides. They are red for the reason the change exists: everything below the hero moves down by the difference between the old band and the new box, which is REAL, INTENDED layout movement, and an instrument reporting zero for it would be broken. The measured shift tracks the height delta almost exactly on every route, which is the evidence that the movement is a rigid translation of the page below the hero and not content reflowing. Coverage falls for the known row-matcher reason witnessed four times in the retired entries: the matcher has no per-pixel tolerance and cannot re-find rows whose luminance a photograph and a gradient changed where a flat black band used to be. RETIREMENT: this harness's baseline IS main, so the merge is the baseline advance and these residuals retire on the first run after the push. THIS ROUTE IS A META-REFRESH STUB (src/gallery.njk) pointing at /saunas/, and the harness follows the redirect, so it captures that page and carries its numbers identically. See the /saunas/ entry. MEASURED, not predicted. /gallery/@1440: changedPct 35.100% (waived), heightDelta +127px (waived), offset +128px, scale x1.000, layoutShiftMaxPx 129 (REFUSED), shiftCoverage 0.507 (REFUSED). /gallery/@390: changedPct 41.912% (waived), heightDelta +124px (waived), layoutShiftMaxPx 125 (REFUSED), shiftCoverage 0.879 (REFUSED).",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/process/",
      "expires": "2026-10-29",
      "reason": "SUB-PAGE HEROES, 2026-09-06, measured ce7732a..84d9b3c. The page opened on a bare text band -- a `.page-hero` with `padding: 12rem 5% 6rem` and nothing in it but the h1 and one line -- and now opens on a 60svh photograph with that same copy on a bottom scrim at the gutter. Doc 10 s2.3 specified this component and s5.3 named it; the 2026-09-03 code-refresh executed the section's two DELETION corollaries and added nothing, which is why ten pages went blank. THE COPY IS UNCHANGED: every h1 and subtitle string is byte-identical, certified by the dom-integrity run at ce7732a..a850d11 (36/36 fingerprints identical modulo 22 measured declarations, PASS). changedPct and heightDelta are waived on that certificate: the pixels moved because a photograph arrived where there was black, and the height moved because the box is taller than the band it replaces. WHAT IS NOT WAIVED, AND MUST NOT BE. layoutShiftMaxPx and shiftCoverage stay RED and are REFUSED RESIDUALS in the form _notes.residualsNotDeclared requires, not waivers and not overrides. They are red for the reason the change exists: everything below the hero moves down by the difference between the old band and the new box, which is REAL, INTENDED layout movement, and an instrument reporting zero for it would be broken. The measured shift tracks the height delta almost exactly on every route, which is the evidence that the movement is a rigid translation of the page below the hero and not content reflowing. Coverage falls for the known row-matcher reason witnessed four times in the retired entries: the matcher has no per-pixel tolerance and cannot re-find rows whose luminance a photograph and a gradient changed where a flat black band used to be. RETIREMENT: this harness's baseline IS main, so the merge is the baseline advance and these residuals retire on the first run after the push. THIS ROUTE IS A META-REFRESH STUB (src/process.njk) pointing at /about/, and the harness follows the redirect, so it captures that page and carries its numbers identically. See the /about/ entry. MEASURED, not predicted. /process/@1440: changedPct 25.767% (waived), heightDelta +126px (waived), offset +127px, scale x1.000, layoutShiftMaxPx 241 (REFUSED), shiftCoverage 0.908 (REFUSED). /process/@390: changedPct 31.838% (waived), heightDelta +124px (waived), layoutShiftMaxPx 124 (REFUSED), shiftCoverage 0.919 (REFUSED).",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    }
  ],
  "pageOverrides": [],
  "expectedRedirects": {
    "/gallery/": "/saunas/",
    "/process/": "/about/"
  },
  "_notes": {
    "authority": "These are the ONLY budgets. lib/gate.mjs reads them and defaults nothing; no threshold is written down anywhere else in the harness. If a budget and a fixture in visual-diff.test.mjs disagree, the fixture wins and the budget is renegotiated in the plan, never tuned here to make a run pass.",
    "widths": "Capture widths in CSS pixels. 1440 = desktop, 390 = iPhone-class mobile. An empty list is a hard error: a run that captures nothing cannot pass.",
    "maxLayoutShiftPx": "Budget from the redesign acceptance criterion. Gated against layoutShiftMaxPx -- the MAXIMUM observed row displacement, not the p99. A 6px sitewide button move touches few enough rows that a percentile hides it, and hiding it is exactly the defect this harness was repaired for.",
    "minShiftCoverage": "Fraction of structured baseline rows that must be found again in the candidate. This is what catches the two failures the pixel and p99 metrics are blind to: content that moved further than diff.mjs's 240px search window, and a page whose content changed outright. Both report a shift of zero; both drop coverage to near zero.",
    "maxChangedPct": "Percentage of the page that may look different before it is treated as a regression. Cosmetic anti-aliasing noise sits well under 0.5%.",
    "maxScaleDivergence": "How far the two capture widths of ONE page may disagree about that page's affine SCALE. Under the affine model this is the sharper of the two divergence checks: both widths render from the same stylesheet, so a leading change should compress them by a comparable ratio even though their layouts differ. 0.05 is a first calibration and should be revisited once real per-page scales from a leading change have been seen; tighten it, do not loosen it, without saying why.",
    "minFitConfidence": "Fraction of a page's ELIGIBLE rows (those whose row signature is distinctive enough to take part in the fit at all -- rows of repeating texture are excluded from both sides of the ratio) that the affine fit must explain before the fit counts as a fit at all. Below this the run FAILS with an explicit model-does-not-fit reason rather than reporting residuals against a fit that does not hold -- if the model is wrong, every shift number beside it is measured against nothing. Deliberately NOT waivable (see UNWAIVABLE in lib/gate.mjs) and NOT overridable per page: a page the instrument cannot describe is a human conversation, not a config line. 0.75 separates the observed cases by a wide margin -- a real leading change fits at 0.958, a constant-offset model on the same page fitted at 0.034 -- but it is a first calibration, not a measured threshold.",
    "maxOffsetDivergencePx": "How far the two capture widths of ONE page may disagree about that page's estimated global vertical offset. Deliberately generous: the 1440px and 390px layouts are different documents and a leading change legitimately compresses them by different amounts, so this is a 'wildly differently' detector rather than a precision gate. The sharp signal alongside it needs no budget at all -- if one width GREW while the other SHRANK, that is reported regardless of magnitude, because no single spacing change does that. 400 is a first calibration and should be revisited once real per-page offsets from a leading change have been seen; tighten it, do not loosen it, without saying why.",
    "expectedToChange": "Per-metric waivers, not a blanket pass. Shape: { \"route\": \"/about/\", \"reason\": \"new hero type scale\", \"waive\": [\"changedPct\"], \"expires\": \"YYYY-MM-DD\" }. expires is MANDATORY as of 2026-08-06, same discipline as pageOverrides: <=90-day horizon, and an EXPIRED entry fails the whole run loudly at loadConfig -- never a silent return to the gate. A lapsed stamp on a preserved entry therefore bricks the suite by design; the deploy checklist carries the re-stamp-or-retire date. Waivable: changedPct, heightDelta, structural. NEVER waivable: layoutShiftMaxPx, shiftCoverage -- a config that tries is a hard error. A page may waive changedPct and remain shift-gated; that is the whole point of the restructure. Bare route strings are rejected so an old-shape config cannot silently keep its old blanket-waiver meaning.",
    "pageOverrides": "Moves ONE named budget on ONE named page, in the direction that metric's TYPE allows, for a stated reason, until a stated date. Shape: { \"page\": \"/about/\", \"metric\": \"maxLayoutShiftPx\", \"value\": 24, \"reason\": \"new hero type scale moves the fold ~20px, approved in the Wave A design review\", \"expires\": \"2026-09-30\" }. CEILINGS (maxLayoutShiftPx, maxChangedPct) may only be RAISED, capped at 100x the global -- loud is fine, infinite is not, so value: 999999 is rejected. The FLOOR (minShiftCoverage) may only be LOWERED, bounded to [0.75, 1) -- lowering is the shippable direction for a deliberate restyle, and below 0.75 a quarter of the page's rows could not be matched at all, which is a human conversation rather than a config line. An override can NEVER disable a metric and can never rescue a page whose shift could not be measured. Rejected on load: unknown metric, wrong direction, out of bounds, missing or empty reason, missing or malformed expiry, an expiry more than 90 days out, a duplicate page/metric pair. An EXPIRED override fails the run loudly rather than lapsing back to the global budget, so a temporary allowance cannot quietly become permanent. Overrides affect only the page and metric they name. This exists so a deliberate restyle is shippable under review; the global budgets above stay unwaivable and are not to be lowered to make a run pass.",
    "expectedRedirects": "Client-side redirects the capture is expected to observe, as route -> settled destination. A redirect that is not declared, points somewhere else, or stops happening fails the run. Without this the harness screenshots the destination page under the source route's name and reports it as unchanged. /gallery/ and /process/ genuinely redirect today (verified 2026-07-30).",
    "wp1b": "WP-1b's recorded coverage figures were measured while the reveal pin was INERT (it still named the six pre-1b class names) and carry unquantifiable observer-race noise on reveal-heavy pages. They are not reproducible under the repaired harness (077ce33) and must not be quoted as a baseline. The batch's conclusions stand on its other evidence (DOM certificate, computed-rhythm suite, isolation probes).",
    "stalePin": "BLOCKER found 2026-07-31 during the Jen Stage 3 certification, and it invalidates this harness's reveal handling for every run since WP-1b. The capture pin at scripts/lib/capture.mjs (the rule that forces every scroll-reveal target to its settled visible state, so below-fold content is captured deterministically) still names the SIX PRE-WP-1b classes: .fade-in, .slide-up, .slide-left, .slide-right, .scale-in, .gallery-item--reveal. WP-1b collapsed all six into .reveal. Measured against the current build: those six selectors match ZERO elements, and the 206 elements carrying .reveal are matched by NOTHING in the pin. The pin has been inert since WP-1b landed. Consequence, stated plainly: below-fold reveal content is captured in whatever state the IntersectionObserver happened to have reached, which is timing-dependent -- exactly the non-determinism the pin exists to remove. Stage 3's F5 changes WHEN the hidden state is applied (parse time instead of DOMContentLoaded), so it shifts that timing and the run reports large diffs on content pages: / 7.6%/20.0%, /saunas/ and /gallery/ 12.3%/39.1%, /faq/ coverage 0.63, /contact/ shift 239px. Pages with no reveal-heavy below-fold content came back clean and are the control: /about/ 0.00%, /process/ 0.00%, /warranty/ 0.00%, /privacy/ 0.00%, /booking-ops.html 0.00% coverage 1.000 -- all PASS. A batch whose CSS-only items are provably DOM-neutral cannot legitimately move /saunas/ by 39% while leaving /about/ at zero. NO expectedToChange entries and NO pageOverrides were written from these numbers. Declaring a measurement taken through a broken instrument as expected is how noise becomes a permanent waiver. The pin is repaired FIRST, as its own reviewed commit by the harness's implementer, and only then is this batch re-measured -- the same rule the plan sets for every other instrument change. Until then the Stage 3 visual certification is OPEN, not passed and not waived. It also reframes the WP-1b record above: that run's coverage collapse was attributed entirely to the rhythm change, and some of it may be this.",
    "residualsNotDeclared": "Three classes from the polish-batch re-measure are deliberately NOT declared, because the mechanism refuses them by design: /faq/@390 coverage 0.627 (below the 0.75 absolute floor - C35 rewrites every question node; a human conversation, had: Jen's rendered review passed it), and fit-confidence failures on /@390 (0.706) and /saunas/ /gallery/@390 (0.565) - fitConfidence is unwaivable and non-overridable. These stay red as truth-tellers until the next baseline advance retires the comparison. ADDITIONALLY: / (home) carries NO shift override even though its 30px@1440 is fully attributed — the F1/F1b calibration fixtures evaluate route '/' against the shipped config, so any home-page shift override disarms the harness's own self-test (this property has now caught bad overrides three times: WP-1a's 18, and the reviewer's+orchestrator's own entry on 2026-07-31). The calibration route stays un-overridden by RULE; home's attributed residual is a truth-teller until the baseline advances.",
    "b4ShiftOverrides": "Wave B-1 B4 deliberately adds NO maxLayoutShiftPx override, although plan §B4 item 6 anticipated one. Measured 2026-08-02: max shift on /saunas/ is 2px at 1440 and 1px at 390, inside the 4px global budget, so an override would be an unconsumed waiver -- which this config's own B6 pruning item calls a live hazard. The plan expected a shift because the page loses ~40% of its height; it does not follow, because what was deleted sat BELOW most of the page's stable content and the affine model absorbs the displacement as offset rather than as per-row shift. The two 40px /saunas/+/gallery/ overrides still in pageOverrides are Wave A's and are now unconsumed too; B6 prunes them with the rest.",
    "b6Prune": "PRUNED 2026-08-02, Wave B-1 batch B6. expectedToChange 18 -> 3, pageOverrides 15 -> 0. WHY: every pruned entry described a change the BASELINE NOW CONTAINS. Wave A shipped, so its type-system, F2/F6/F7, C2/C4/C35 and ruling-(d) deltas are on both sides of every comparison and the waivers they justified are unconsumed. An unconsumed waiver is not inert -- it is a live hazard, because it silently covers the NEXT change to that page/metric, which nobody reviewed. This config's own pageOverrides note calls that out and b4ShiftOverrides names the two 40px /saunas/+/gallery/ entries as Wave A's, now unconsumed, to be pruned here with the rest. PRESERVED, deliberately, three expectedToChange entries whose changes are NOT yet in the baseline: '/' (B1's rewritten LCP/sizes entry) and '/saunas/' + '/gallery/' (B4's recomposition entries, re-measured 2026-08-02). Those retire at the Deploy-1 baseline advance, not here. PRUNED expectedToChange routes: /404.html, /about/, /book/, /contact/, /contact/thank-you/, /faq/, /locations/, /north-shore/, /privacy/, /process/, /sea-to-sky/, /squamish/, /vancouver/, /warranty/, /whistler/. PRUNED pageOverrides: /contact/:maxLayoutShiftPx, /contact/:minShiftCoverage, /locations/:maxLayoutShiftPx, /locations/:minShiftCoverage, /404.html:minShiftCoverage, /north-shore/:minShiftCoverage, /sea-to-sky/:minShiftCoverage, /vancouver/:minShiftCoverage, /squamish/:minShiftCoverage, /whistler/:minShiftCoverage, /book/:maxLayoutShiftPx, /contact/thank-you/:maxLayoutShiftPx, /saunas/:maxLayoutShiftPx, /gallery/:maxLayoutShiftPx, /faq/:maxLayoutShiftPx. FIXTURE SAFETY, checked before pruning rather than after: the F-series calibrates route '/' against the SHIPPED config (visual-diff.test.mjs:627/634) and P6 asserts no shipped override names '/'; pruning to zero overrides keeps both satisfied, and the '/' expectedToChange entry that F1 exercises is preserved. Every other config-reading fixture builds its own synthetic config (see the explicit comment at :759). The 'zero shipped overrides' invariant was retired earlier as Batch-1-scoped truth, so an empty pageOverrides list is a legal state, not a regression.",
    "subPageHeroRetirement20260906": "RETIREMENT OF THE 18 expectedToChange ENTRIES, 2026-09-06, its own commit ahead of the sub-page hero waivers. Every one of the eighteen described a change that is now IN main -- Wave B-1 batch B1, the B4 /saunas/ recomposition, the code-refresh, the Lee review, hero composition C -- and this harness's baseline IS main, so each entry's own retirement clause ('the merge to main is itself the baseline advance and the residual retires on the first run after the push') has been satisfied by the merges that already happened. They were not expired: all eighteen carried expires 2026-10-05 and would have kept excusing changedPct and heightDelta on eighteen routes for another month, which is the exact 'load-bearing forever' failure the authority note names. Retired as a separate commit from the seven fresh hero entries so that the two movements are separable in history and the F1/F1b calibration fixtures are proved against the pruned config before anything new is added -- in particular that retiring the '/' entry does not disarm the harness's own self-test, since P6 forbids '/' ever carrying a pageOverride and F1 asserts .some() on the shift reason. visual-diff:test after this prune: 164 passed, 0 failed. NOTHING THAT WAS REFUSED IS NOW WAIVED: every one of these entries waived changedPct and heightDelta only (plus `structural` on /book/); the shift and fitConfidence residuals recorded in their reasons were refused, never waived, and retiring the entries leaves them exactly as refused as they were.",
    "subPageHeroWaivers20260906": "NINE ENTRIES, SEVEN PAGES. The plan says seven; the harness treats /gallery/ and /process/ as routes, and they are meta-refresh stubs the capture follows to /saunas/ and /about/, so they report those pages' numbers and need their own entries or they fail undeclared. Each waives changedPct and heightDelta ONLY. Every shift and coverage red is a refused residual carried verbatim in the entry's own reason with its measured number, per _notes.residualsNotDeclared -- no budget in this file was edited to make a run pass. EXPIRES 2026-10-29, AND THE DATE IS NOT FREELY CHOSEN. The 90-day horizon guard is evaluated against the SHIPPED config by the P-section fixture in visual-diff.test.mjs, which pins its clock to 2026-08-01T12:00Z (scripts/visual-diff.test.mjs, `const NOW`) and spreads the shipped entries into its own test.json. So the practical ceiling for anything in this file is ~2026-10-30, measured from that frozen instant, not from the day the entry is written. 2026-12-05, 2026-12-04 and 2026-12-01 were each rejected by that fixture, which is how this was found. Worth recording as its own small defect: a fixture with a frozen NOW that validates the LIVE config makes the file's usable expiry window shrink by one day for every day that passes, and it will eventually reject every legal date. Not fixed here -- it is not this relay's file to change -- but it should be, and the fix is for the fixture to substitute its own synthetic expiries rather than inherit the shipped ones, which is exactly what its own comment at :39-42 says the synthetic dates exist to avoid."
  }
}
