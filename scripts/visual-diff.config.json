{
  "widths": [
    1440,
    390
  ],
  "maxLayoutShiftPx": 4,
  "minShiftCoverage": 0.95,
  "maxChangedPct": 0.5,
  "maxOffsetDivergencePx": 400,
  "maxScaleDivergence": 0.05,
  "minFitConfidence": 0.75,
  "expectedToChange": [
    {
      "route": "/",
      "expires": "2026-10-05",
      "reason": "Wave B-1 batch B1, LCP delivery. The homepage hero <img> gains a hero-scoped sizes=\"100vw\" in place of the build transform's default (which tops out at 800px and describes a contained card), and the preload gains an imagesrcset/imagesizes generated from the same candidate list the element carries. The browser therefore selects a correctly-sized candidate for the LCP photograph -- MEASURED in a real browser against the built site: w_1920 at 1440 and w_400 at 390, exactly one hero request per width, where it previously fetched w_1200 for the preload and w_800 for the element. WHAT THE HARNESS ACTUALLY MEASURED (not predicted): changedPct 0.000% at 1440 and 0.001% at 390 -- the re-resampled photograph is below the metric's per-pixel tolerance, so this waiver consumes almost nothing and is kept as the declaration of intent rather than as cover. Height delta 0 at both widths. WHY THIS SHAPE AND NOT A pageOverride: route '/' may never carry a pageOverride. The F1/F1b calibration fixtures evaluate '/' against the shipped config, so a home-page override disarms the harness's own self-test -- a property that has caught bad '/' entries three times (see _notes.residualsNotDeclared). A changedPct waiver is the compliant shape precisely BECAUSE the shift gates are unwaivable and stay live. THEY STAY LIVE AND THEY ARE RED: layoutShiftMaxPx 51 at 1440 and shiftCoverage 0.926 at 390. That is recorded here as a red, NOT waived and NOT overridden -- per _notes.authority, a budget and a fixture that disagree are renegotiated in the plan, never tuned here to make a run pass. The evidence that it is not layout movement, measured pixel-by-pixel on the two captures: the rows that differ are 0-899 at 1440 and 0-719 at 390, i.e. exactly the 100vh hero band, and EVERY row below the hero is byte-identical, with total page height unchanged (7036 and 10635) and max per-channel delta 33/38. Nothing on the page moved; the row-signature matcher has no tolerance for a re-resampled photograph and mismatches rows inside the hero band. Escalated to the relay rather than absorbed. Retirement: tracked in the ROADMAP, executed by B6, which must PRESERVE this entry until the baseline advances past it. (EXPIRY AMENDMENT 2026-08-06: the schema gained a mandatory expires field for expectedToChange in the code-refresh B1 batch — the clause that stood here, saying no such field existed, stopped being true that day. This entry is stamped 2026-10-05; re-stamp or retire at the deploy-time baseline advance, whichever comes first.) DEPLOY-1 FIX ROUND ADDENDUM (2026-08-02), two further reasons `/` moves, both deliberate and neither one waiving anything new. (1) THE HERO SCRIM: --hero-scrim was declared at styles.css:169 and painted by nothing -- .hero-content's comment claimed contrast was 'carried by the scrim' while the only thing carrying it was filter: brightness(0.7). Jen's Stage 3 ruling puts it on .hero::after. Measured against the brightest pixel behind each element at 1440, h1 goes 1.77:1 -> 5.86:1 and the subtitle 1.79:1 -> 5.89:1. This is PAINT ONLY -- an inset:0 pseudo-element with pointer-events:none, no box in flow -- so it moves changedPct at both widths and moves NOTHING in layout. The shift gates stay live against it and must. (2) THE HERO CLEARANCE FIX, which is layout and is confined to <=768px: the hero now reserves the fixed nav's band (--nav-band) and centres in what is left, because it had been centring into a strip whose top 91px the nav paints over, putting the h1 behind the nav at 360-430. At 1440 the rule does not apply and geometry is byte-for-byte unchanged -- the 1440 capture moves for the scrim alone. At 390 the hero band moves down 45.5px; page height is UNCHANGED at 390 (the box stays 80vh and the content re-centres) and grows only at 360, which is not a captured width. NOTE PRECISELY what this does to the shift reds recorded above: they were already red and already REFUSED, and the rows they implicate are the hero band (0-719 at 390), which is exactly the band this fix moves. They stay red, they stay refused, and they are NOT re-litigated, re-waived or overridden here -- `/` still carries no pageOverride, by the rule stated above. What changes is that part of the 390 residual now has an intended layout cause on top of the resampling one. Retirement remains B6's, at baseline advance. MEASURED AFTERWARDS, 5748de8..b1576e1, replacing the prediction above with the run's own numbers. Of 38 page/width comparisons exactly TWO carry any changed pixels and both are `/`; the other 36 are 0.000%, so the wave's pixel-control property is intact and neither fix leaked off this route. `/`@1440: changedPct 7.519% (waived), layoutShiftMaxPx 0, heightDelta 0, shiftCoverage 0.888. `/`@390: changedPct 4.188% (waived), layoutShiftMaxPx 60, heightDelta 0, shiftCoverage 0.984. Read the 1440 row carefully -- shift 0 and height 0 are the direct confirmation that the scrim is paint only, since 1440 is the width where the clearance rule does not apply. THE TWO REDS SWAPPED METRIC AND WIDTH and that is worth stating so nobody reads it as a gate newly broken or newly excused. Before this round they were layoutShiftMaxPx 51 at 1440 and shiftCoverage 0.926 at 390. Now they are shiftCoverage 0.888 at 1440 and layoutShiftMaxPx 60 at 390. The 1440 red is the same instrument limitation as before wearing a different name: nothing moved there, but the row-signature matcher has no per-pixel tolerance and cannot re-find rows whose luminance the scrim changed, which is the row-matcher tolerance issue already queued post-wave. The 390 red is REAL LAYOUT and intended -- the hero content moves down 45.5px to clear the nav -- and 390's coverage incidentally recovered from 0.926 to 0.984. Both remain REFUSED RESIDUALS: nothing here is waived, overridden or re-litigated, `/` still carries no pageOverride, and retirement is still B6's at baseline advance. B4 FIX-ROUND ADDENDUM (2026-08-02, Razor W4). This route gains one more intended change: home.njk:31's capacity copy, the FIFTH drifted SC figure, corrected from '14-person+ commercial units' to '12-person+' -- canonical. It is TEXT, and the concern raised before measuring was that a text change on the calibration route would push changedPct past what this waiver was written for. MEASURED, ab83ed6..e791132: 0.000% at 1440 and 0.001% at 390, layoutShiftMaxPx 0, heightDelta 0, and the route PASSES. A two-character change inside a paragraph is below the metric's per-pixel tolerance, so the waiver consumes essentially nothing on its account and nothing new is excused. Recorded because the change is real and deliberate even though the instrument cannot see it -- an undocumented invisible change is still an undocumented change. The shift gates stayed live throughout and reported 0. CODE-REFRESH ADDENDUM (2026-08-06). This route takes two further intended changes: the D7a copy fix (the Try-a-Session card advertised booking at 'our Aldergrove location' -- a live false claim about where the business operates; replaced with doc 13's approved venue-neutral sentence verbatim) and the D6 colophon link. MEASURED 7b506b5..59b8a12: 1440 is 0.036% changed, shift 0, height 0 and PASSES. 390 is 10.233% changed with height 0 and a layoutShiftMaxPx that MOVED BETWEEN RUNS -- 24px on one run, 73px on the next, same two commits. That instability is itself the finding and is recorded, not smoothed: it is the known row-matcher tolerance issue (the matcher has no per-pixel tolerance and cannot re-find rows whose content changed, so its residuals on a text reflow are not a stable measurement of movement), already queued post-wave and now with a second witness -- the replacement sentence is shorter and reflows the card stack at mobile width. The shift red is a REFUSED RESIDUAL like the ones above it: real, intended, measured, not waived and not overridden -- '/' still carries no pageOverride, by the calibration-route rule. LEE-REVIEW ADDENDUM (2026-08-07), measured 7b506b5..6149351. The hero stage became position:sticky over a 70vh runway so the title stops leaving on the same scroll that reveals it (Lee's finding; >=768px only, reduced-motion opts out). MEASURED: 1440 is 8.19% changed with layoutShiftMaxPx 0 and heightDelta 0 -- the photograph renders at its stuck position in a full-page capture, which is a compositing difference, not movement, and the shift gate reporting ZERO is the evidence for that. 390 is 44.47% with shift 127 and height +271: the runway does not apply below 768, so that delta is the D7a copy fix plus the colophon link reflowing the card stack. REFUSED RESIDUAL, not waived: the 390 shift is real intended layout and stays red -- '/' still carries no pageOverride, by the calibration-route rule. HERO COMPOSITION C ADDENDUM (2026-09-04), measured 69328a1..c1fd3bb. The hero is recomposed: brightness(0.7) deleted from the photograph, the permanent full-bleed scrim replaced by a bottom-only gradient that is ABSENT during the 1.6s hold and fades in on the held pair's own stagger slot, the text block moved from optically centred to bottom-left at the page gutter, the subtitle cut to one sentence, one of the two buttons removed. Below 768 the three stage items also become calc(100svh - var(--nav-band)) so a bottom-anchored button is not parked under the iOS toolbar, and the desktop bottom inset carries --section-pad because the hero is a bare <section> whose rhythm padding used to cancel for centred content and does not cancel for anchored content. MEASURED, not predicted. Of 36 page/width comparisons exactly TWO carry any changed pixels and both are `/`; every other route reports 0.000%, so nothing leaked off this route. `/`@1440: changedPct 4.474% (waived), heightDelta 0, offset 0px, scale x1.000, layoutShiftMaxPx 417, shiftCoverage 0.868. `/`@390: changedPct 31.225% (waived), heightDelta -91px (waived), offset -90px, scale x1.000, layoutShiftMaxPx 196, shiftCoverage 0.897. NO NEW WAIVER IS ADDED. The two waivable metrics that moved, changedPct and heightDelta, were already in this entry's waive array, and the -91px at 390 is exactly --nav-band: the svh change doing precisely what it was written to do and nothing else. THE FOUR SHIFT NUMBERS ARE REFUSED RESIDUALS, in the same form as the ones above them: layoutShiftMaxPx and shiftCoverage are unwaivable by construction and they stay RED, not waived, not overridden, and `/` still carries no pageOverride by the calibration-route rule. They are red for the reason the change exists rather than for a defect in it: a text block that moves from the middle of the frame to its bottom-left corner IS layout movement, deliberately, and an instrument that reported zero for it would be broken. The 1440 shift of 417px is about the distance from the vertical centre of the stage to a block sitting 3.5rem off its visible floor; the 390 shift of 196px is the same move against a shorter stage that also lost the nav band. Coverage falls at both widths because the row-signature matcher has no per-pixel tolerance and cannot re-find rows whose luminance the removed brightness filter changed -- the known row-matcher issue already witnessed twice above, here with a third witness. RETIREMENT NEEDS NO FLAG AND NO NEW WORK: this harness's baseline IS main, so the merge to main is itself the baseline advance and the residual retires on the first run after the push. Nothing is owed at Stage 5 beyond re-running the harness and confirming `/` reports clean against the new main. Expiry stays 2026-10-05 with the others so nothing is orphaned. ONE MEASUREMENT THIS HARNESS DOES NOT MAKE, recorded here because it is the open question this change leaves: hero TYPE contrast. Sampling the brightest pixel inside each element's box on the settled capture -- the same method that recorded 1.77:1 -> 5.86:1 when the scrim was first painted -- the h1 is now 1.55:1 at 1440 and 1.27:1 at 390, the subtitle 1.69 and 1.34, the button 1.78 and 1.51. NAV contrast is fine and was the thing the plan asked to be measured (6.57:1 to 11.92:1 at both widths, so no top gradient was added). The hero type is not fine, and the cause is structural rather than a mistake in the values: variant-c was a ONE-LINE hero sitting at the bottom inset where the gradient is strongest, and this is a three-element block whose headline sits about 200px higher, in the part of the gradient that is still nearly transparent. Escalated to Jen at Stage 3 rather than fixed here, because how dark and with what stops is a composition ruling, not a mechanical correction.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/gallery/",
      "expires": "2026-10-05",
      "reason": "B4 /saunas/ RECOMPOSITION, re-measured 2026-08-02 against ab83ed6 (supersedes the WP-1a type-system reason this entry carried, which described a different change and was left stale by that batch). The page is reordered and roughly 40% of it deleted: the three-card comparison section with its 21 tick/cross bullets, the three-stat science wall, the five model CARDS (replaced by five ruled ledger rows carrying no images), and the 'Commercial & Custom' section. MEASURED: 54.623% of pixels changed at 1440 with the page 1576px shorter, 63.865% at 390 with it 4702px shorter. Content is deliberately lost here -- that is the change -- so the 'no content lost' argument the old reason made does NOT apply, and the certificate that content went where it was supposed to is the DOM-integrity run instead: 38/38 fingerprints identical modulo 14 measured declarations, PASS. changedPct and heightDelta are waived on that evidence. WHAT IS NOT WAIVED, and must not be: fitConfidence (0.534@1440, 0.632@390), shift coverage (0.149@1440, 0.373@390) and the 3062px width-offset spread stay RED. They are correct. A recomposition removes far more vertical space at 390 than at 1440, so no single scale-and-offset describes the page, and the harness refuses to pretend one does -- the same class of honest does-not-fit verdict the DOM module documents for a typeface swap ('that verdict is the product, not a defect'). They retire on the next baseline advance. Do not chase them and do not add overrides for them; fitConfidence is unwaivable by design. (This route renders /saunas/ content; /gallery/ redirects there.) LEE-REVIEW ADDENDUM (2026-08-07). This route is the meta-refresh stub that renders /saunas/, so it carries that page's changes identically -- same thumbnails, same three-stat set, same measurements (33.39%/+165 at 1440, 39.75%/+424 at 390, shift 166 refused not waived). See the /saunas/ entry for the argument.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/saunas/",
      "expires": "2026-10-05",
      "reason": "B4 /saunas/ RECOMPOSITION, re-measured 2026-08-02 against ab83ed6 (supersedes the WP-1a type-system reason this entry carried, which described a different change and was left stale by that batch). The page is reordered and roughly 40% of it deleted: the three-card comparison section with its 21 tick/cross bullets, the three-stat science wall, the five model CARDS (replaced by five ruled ledger rows carrying no images), and the 'Commercial & Custom' section. MEASURED: 54.623% of pixels changed at 1440 with the page 1576px shorter, 63.865% at 390 with it 4702px shorter. Content is deliberately lost here -- that is the change -- so the 'no content lost' argument the old reason made does NOT apply, and the certificate that content went where it was supposed to is the DOM-integrity run instead: 38/38 fingerprints identical modulo 14 measured declarations, PASS. changedPct and heightDelta are waived on that evidence. WHAT IS NOT WAIVED, and must not be: fitConfidence (0.534@1440, 0.632@390), shift coverage (0.149@1440, 0.373@390) and the 3062px width-offset spread stay RED. They are correct. A recomposition removes far more vertical space at 390 than at 1440, so no single scale-and-offset describes the page, and the harness refuses to pretend one does -- the same class of honest does-not-fit verdict the DOM module documents for a typeface swap ('that verdict is the product, not a defect'). They retire on the next baseline advance. Do not chase them and do not add overrides for them; fitConfidence is unwaivable by design. LEE-REVIEW ADDENDUM (2026-08-07), measured 7b506b5..6149351. Two intended changes: a photograph per model-index row (the frame the configurator opens on, gated by model-thumbs.test.mjs) and the science block going from one stat to three (Jen's archetype B overridden by Lee; the restored claims are verbatim with their original sources). MEASURED: 33.39% / height +165 at 1440, 39.75% / height +424 at 390. REFUSED RESIDUAL: layoutShiftMaxPx 166 at 1440 stays RED and is not waived -- adding five thumbnails and two stat blocks moves everything below them by exactly their height, which is intended layout movement and is recorded as such.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/404.html",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/about/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/book/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta",
        "structural"
      ],
      "reason": "REPURPOSED 2026-09-02: this route is DELETED, not restyled. /book/ was a placeholder rendering a fire-ban closure notice; real booking is the separate app at book.secretsaunacompany.ca, which the nav, hero and two cards already link to directly, so the page's only remaining function was telling anyone who arrived from Google that bookings were shut months after they reopened. src/book.njk and pages/coming-soon.njk are gone and netlify.toml 301s /book and /book/ to the app. `structural` is added because the route disappears from the candidate capture and that is the declaration the harness needs; the ORIGINAL reason for this entry (the 2026-08-06 footer Privacy link, measured byte-clean at 1440 and +23px at 390) is retired by the deletion rather than by a baseline advance. The shift gates stay live and unwaived, as always. Note the redirect is a SERVER 301, so it does not belong in expectedRedirects, which declares client-side redirects the capture observes."
    },
    {
      "route": "/contact/thank-you/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/faq/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/locations/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/north-shore/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/privacy/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/process/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/sea-to-sky/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/squamish/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/vancouver/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/warranty/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/whistler/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D6. The footer colophon gains a Privacy link beside the copyright (/privacy/ had been reachable ONLY from inside the quote modal, which ships on /saunas/ alone since the modal-scoping batch -- undiscoverable from 18 of 19 routes on a PIPA-conscious site). At 1440 the link fits the copyright line and this route is byte-clean: MEASURED 0.02-0.09% changed, shift 0, height 0. At 390 it wraps to a second colophon line: height +23px, changed 0.28-1.17%, shift 0 -- one line of --text-sm, the honest cost of the link. The SHIFT gates stay live and report 0 here, which is the evidence that nothing above the footer moved. Retire at the deploy baseline advance."
    },
    {
      "route": "/contact/",
      "expires": "2026-10-05",
      "waive": [
        "changedPct",
        "heightDelta"
      ],
      "reason": "code-refresh 2026-08-06, D5 + D6. /contact/ gains two blocks under the submit button: the inline failure surface (#contactError, hidden until a send fails -- replacing a blocking alert() that left no trace, no error code and no mailto escape) and the US-processor disclosure paragraph (the quote modal's sentence verbatim; the form had been collecting name, email and phone for the same US endpoint with no notice). MEASURED: 2.928% at 1440 and 5.387% at 390, height +60/+83px. REFUSED RESIDUAL, recorded not waived, and the FINAL run reports it as a MODEL-DOES-NOT-FIT rather than a shift number: the affine fit explains only 62.2% of rows at 1440 and 53.5% at 390, under the 75% floor, because inserting two blocks mid-page is not a single scale-and-offset transformation and no affine model can describe it. The harness says so itself -- 'not a pass and not a measured regression' -- and that verdict is the product, recorded rather than overridden -- the shift gates are unwaivable by design and this IS real layout movement, content below the insertion point moving down by exactly the height of what was inserted. It is intended, it is bounded, and it is declared here rather than excused: per _notes.authority a budget and a fixture that disagree are renegotiated in the plan, never tuned here. LEE-REVIEW ADDENDUM (2026-08-07). The section holding the form takes the tight tier via :has(), closing a 270px gap between 'Get Your Quote' and the first field it asks for (168px now; 214->137 at 390). MEASURED with the D5/D6 additions already in this entry: 6.19% at 1440 with height -96 (the page got SHORTER -- the spacing removed exceeds the error surface and disclosure added) and 16.59% at 390 with height -23. REFUSED RESIDUAL: layoutShiftMaxPx 109/78 stays RED and unwaived -- removing 102px of padding above the form moves the form and everything under it up by that much. Intended, bounded, recorded."
    }
  ],
  "pageOverrides": [],
  "expectedRedirects": {
    "/gallery/": "/saunas/",
    "/process/": "/about/"
  },
  "_notes": {
    "authority": "These are the ONLY budgets. lib/gate.mjs reads them and defaults nothing; no threshold is written down anywhere else in the harness. If a budget and a fixture in visual-diff.test.mjs disagree, the fixture wins and the budget is renegotiated in the plan, never tuned here to make a run pass.",
    "widths": "Capture widths in CSS pixels. 1440 = desktop, 390 = iPhone-class mobile. An empty list is a hard error: a run that captures nothing cannot pass.",
    "maxLayoutShiftPx": "Budget from the redesign acceptance criterion. Gated against layoutShiftMaxPx -- the MAXIMUM observed row displacement, not the p99. A 6px sitewide button move touches few enough rows that a percentile hides it, and hiding it is exactly the defect this harness was repaired for.",
    "minShiftCoverage": "Fraction of structured baseline rows that must be found again in the candidate. This is what catches the two failures the pixel and p99 metrics are blind to: content that moved further than diff.mjs's 240px search window, and a page whose content changed outright. Both report a shift of zero; both drop coverage to near zero.",
    "maxChangedPct": "Percentage of the page that may look different before it is treated as a regression. Cosmetic anti-aliasing noise sits well under 0.5%.",
    "maxScaleDivergence": "How far the two capture widths of ONE page may disagree about that page's affine SCALE. Under the affine model this is the sharper of the two divergence checks: both widths render from the same stylesheet, so a leading change should compress them by a comparable ratio even though their layouts differ. 0.05 is a first calibration and should be revisited once real per-page scales from a leading change have been seen; tighten it, do not loosen it, without saying why.",
    "minFitConfidence": "Fraction of a page's ELIGIBLE rows (those whose row signature is distinctive enough to take part in the fit at all -- rows of repeating texture are excluded from both sides of the ratio) that the affine fit must explain before the fit counts as a fit at all. Below this the run FAILS with an explicit model-does-not-fit reason rather than reporting residuals against a fit that does not hold -- if the model is wrong, every shift number beside it is measured against nothing. Deliberately NOT waivable (see UNWAIVABLE in lib/gate.mjs) and NOT overridable per page: a page the instrument cannot describe is a human conversation, not a config line. 0.75 separates the observed cases by a wide margin -- a real leading change fits at 0.958, a constant-offset model on the same page fitted at 0.034 -- but it is a first calibration, not a measured threshold.",
    "maxOffsetDivergencePx": "How far the two capture widths of ONE page may disagree about that page's estimated global vertical offset. Deliberately generous: the 1440px and 390px layouts are different documents and a leading change legitimately compresses them by different amounts, so this is a 'wildly differently' detector rather than a precision gate. The sharp signal alongside it needs no budget at all -- if one width GREW while the other SHRANK, that is reported regardless of magnitude, because no single spacing change does that. 400 is a first calibration and should be revisited once real per-page offsets from a leading change have been seen; tighten it, do not loosen it, without saying why.",
    "expectedToChange": "Per-metric waivers, not a blanket pass. Shape: { \"route\": \"/about/\", \"reason\": \"new hero type scale\", \"waive\": [\"changedPct\"], \"expires\": \"YYYY-MM-DD\" }. expires is MANDATORY as of 2026-08-06, same discipline as pageOverrides: <=90-day horizon, and an EXPIRED entry fails the whole run loudly at loadConfig -- never a silent return to the gate. A lapsed stamp on a preserved entry therefore bricks the suite by design; the deploy checklist carries the re-stamp-or-retire date. Waivable: changedPct, heightDelta, structural. NEVER waivable: layoutShiftMaxPx, shiftCoverage -- a config that tries is a hard error. A page may waive changedPct and remain shift-gated; that is the whole point of the restructure. Bare route strings are rejected so an old-shape config cannot silently keep its old blanket-waiver meaning.",
    "pageOverrides": "Moves ONE named budget on ONE named page, in the direction that metric's TYPE allows, for a stated reason, until a stated date. Shape: { \"page\": \"/about/\", \"metric\": \"maxLayoutShiftPx\", \"value\": 24, \"reason\": \"new hero type scale moves the fold ~20px, approved in the Wave A design review\", \"expires\": \"2026-09-30\" }. CEILINGS (maxLayoutShiftPx, maxChangedPct) may only be RAISED, capped at 100x the global -- loud is fine, infinite is not, so value: 999999 is rejected. The FLOOR (minShiftCoverage) may only be LOWERED, bounded to [0.75, 1) -- lowering is the shippable direction for a deliberate restyle, and below 0.75 a quarter of the page's rows could not be matched at all, which is a human conversation rather than a config line. An override can NEVER disable a metric and can never rescue a page whose shift could not be measured. Rejected on load: unknown metric, wrong direction, out of bounds, missing or empty reason, missing or malformed expiry, an expiry more than 90 days out, a duplicate page/metric pair. An EXPIRED override fails the run loudly rather than lapsing back to the global budget, so a temporary allowance cannot quietly become permanent. Overrides affect only the page and metric they name. This exists so a deliberate restyle is shippable under review; the global budgets above stay unwaivable and are not to be lowered to make a run pass.",
    "expectedRedirects": "Client-side redirects the capture is expected to observe, as route -> settled destination. A redirect that is not declared, points somewhere else, or stops happening fails the run. Without this the harness screenshots the destination page under the source route's name and reports it as unchanged. /gallery/ and /process/ genuinely redirect today (verified 2026-07-30).",
    "wp1b": "WP-1b's recorded coverage figures were measured while the reveal pin was INERT (it still named the six pre-1b class names) and carry unquantifiable observer-race noise on reveal-heavy pages. They are not reproducible under the repaired harness (077ce33) and must not be quoted as a baseline. The batch's conclusions stand on its other evidence (DOM certificate, computed-rhythm suite, isolation probes).",
    "stalePin": "BLOCKER found 2026-07-31 during the Jen Stage 3 certification, and it invalidates this harness's reveal handling for every run since WP-1b. The capture pin at scripts/lib/capture.mjs (the rule that forces every scroll-reveal target to its settled visible state, so below-fold content is captured deterministically) still names the SIX PRE-WP-1b classes: .fade-in, .slide-up, .slide-left, .slide-right, .scale-in, .gallery-item--reveal. WP-1b collapsed all six into .reveal. Measured against the current build: those six selectors match ZERO elements, and the 206 elements carrying .reveal are matched by NOTHING in the pin. The pin has been inert since WP-1b landed. Consequence, stated plainly: below-fold reveal content is captured in whatever state the IntersectionObserver happened to have reached, which is timing-dependent -- exactly the non-determinism the pin exists to remove. Stage 3's F5 changes WHEN the hidden state is applied (parse time instead of DOMContentLoaded), so it shifts that timing and the run reports large diffs on content pages: / 7.6%/20.0%, /saunas/ and /gallery/ 12.3%/39.1%, /faq/ coverage 0.63, /contact/ shift 239px. Pages with no reveal-heavy below-fold content came back clean and are the control: /about/ 0.00%, /process/ 0.00%, /warranty/ 0.00%, /privacy/ 0.00%, /booking-ops.html 0.00% coverage 1.000 -- all PASS. A batch whose CSS-only items are provably DOM-neutral cannot legitimately move /saunas/ by 39% while leaving /about/ at zero. NO expectedToChange entries and NO pageOverrides were written from these numbers. Declaring a measurement taken through a broken instrument as expected is how noise becomes a permanent waiver. The pin is repaired FIRST, as its own reviewed commit by the harness's implementer, and only then is this batch re-measured -- the same rule the plan sets for every other instrument change. Until then the Stage 3 visual certification is OPEN, not passed and not waived. It also reframes the WP-1b record above: that run's coverage collapse was attributed entirely to the rhythm change, and some of it may be this.",
    "residualsNotDeclared": "Three classes from the polish-batch re-measure are deliberately NOT declared, because the mechanism refuses them by design: /faq/@390 coverage 0.627 (below the 0.75 absolute floor - C35 rewrites every question node; a human conversation, had: Jen's rendered review passed it), and fit-confidence failures on /@390 (0.706) and /saunas/ /gallery/@390 (0.565) - fitConfidence is unwaivable and non-overridable. These stay red as truth-tellers until the next baseline advance retires the comparison. ADDITIONALLY: / (home) carries NO shift override even though its 30px@1440 is fully attributed — the F1/F1b calibration fixtures evaluate route '/' against the shipped config, so any home-page shift override disarms the harness's own self-test (this property has now caught bad overrides three times: WP-1a's 18, and the reviewer's+orchestrator's own entry on 2026-07-31). The calibration route stays un-overridden by RULE; home's attributed residual is a truth-teller until the baseline advances.",
    "b4ShiftOverrides": "Wave B-1 B4 deliberately adds NO maxLayoutShiftPx override, although plan §B4 item 6 anticipated one. Measured 2026-08-02: max shift on /saunas/ is 2px at 1440 and 1px at 390, inside the 4px global budget, so an override would be an unconsumed waiver -- which this config's own B6 pruning item calls a live hazard. The plan expected a shift because the page loses ~40% of its height; it does not follow, because what was deleted sat BELOW most of the page's stable content and the affine model absorbs the displacement as offset rather than as per-row shift. The two 40px /saunas/+/gallery/ overrides still in pageOverrides are Wave A's and are now unconsumed too; B6 prunes them with the rest.",
    "b6Prune": "PRUNED 2026-08-02, Wave B-1 batch B6. expectedToChange 18 -> 3, pageOverrides 15 -> 0. WHY: every pruned entry described a change the BASELINE NOW CONTAINS. Wave A shipped, so its type-system, F2/F6/F7, C2/C4/C35 and ruling-(d) deltas are on both sides of every comparison and the waivers they justified are unconsumed. An unconsumed waiver is not inert -- it is a live hazard, because it silently covers the NEXT change to that page/metric, which nobody reviewed. This config's own pageOverrides note calls that out and b4ShiftOverrides names the two 40px /saunas/+/gallery/ entries as Wave A's, now unconsumed, to be pruned here with the rest. PRESERVED, deliberately, three expectedToChange entries whose changes are NOT yet in the baseline: '/' (B1's rewritten LCP/sizes entry) and '/saunas/' + '/gallery/' (B4's recomposition entries, re-measured 2026-08-02). Those retire at the Deploy-1 baseline advance, not here. PRUNED expectedToChange routes: /404.html, /about/, /book/, /contact/, /contact/thank-you/, /faq/, /locations/, /north-shore/, /privacy/, /process/, /sea-to-sky/, /squamish/, /vancouver/, /warranty/, /whistler/. PRUNED pageOverrides: /contact/:maxLayoutShiftPx, /contact/:minShiftCoverage, /locations/:maxLayoutShiftPx, /locations/:minShiftCoverage, /404.html:minShiftCoverage, /north-shore/:minShiftCoverage, /sea-to-sky/:minShiftCoverage, /vancouver/:minShiftCoverage, /squamish/:minShiftCoverage, /whistler/:minShiftCoverage, /book/:maxLayoutShiftPx, /contact/thank-you/:maxLayoutShiftPx, /saunas/:maxLayoutShiftPx, /gallery/:maxLayoutShiftPx, /faq/:maxLayoutShiftPx. FIXTURE SAFETY, checked before pruning rather than after: the F-series calibrates route '/' against the SHIPPED config (visual-diff.test.mjs:627/634) and P6 asserts no shipped override names '/'; pruning to zero overrides keeps both satisfied, and the '/' expectedToChange entry that F1 exercises is preserved. Every other config-reading fixture builds its own synthetic config (see the explicit comment at :759). The 'zero shipped overrides' invariant was retired earlier as Batch-1-scoped truth, so an empty pageOverrides list is a legal state, not a regression."
  }
}
