{
  "widths": [
    1440,
    390
  ],
  "maxLayoutShiftPx": 4,
  "minShiftCoverage": 0.95,
  "maxChangedPct": 0.5,
  "maxOffsetDivergencePx": 400,
  "maxScaleDivergence": 0.05,
  "minFitConfidence": 0.75,
  "expectedToChange": [
    {
      "route": "/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 25.6% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/404.html",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 9.5% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/about/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 28.2% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/book/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 11.3% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/contact/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 9.6% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/contact/thank-you/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 9.8% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/faq/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 10.8% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/gallery/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 47.0% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/locations/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 34.0% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/north-shore/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 15.1% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/privacy/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 12.9% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/process/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 28.2% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/saunas/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 47.0% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/sea-to-sky/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 17.1% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/squamish/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 17.0% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/vancouver/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 15.1% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/warranty/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 15.5% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    },
    {
      "route": "/whistler/",
      "reason": "WP-1a type system: body line-height 1.8 -> 1.65 and ~20 ad-hoc sizes replaced by the token scale. Recolours nothing; 12.7% of pixels differ because almost every text run re-set. Verified no content lost: DOM text length, element count, paragraph count and image count are identical to the baseline.",
      "waive": [
        "changedPct",
        "heightDelta"
      ]
    }
  ],
  "pageOverrides": [],
  "expectedRedirects": {
    "/gallery/": "/saunas/",
    "/process/": "/about/"
  },
  "_notes": {
    "authority": "These are the ONLY budgets. lib/gate.mjs reads them and defaults nothing; no threshold is written down anywhere else in the harness. If a budget and a fixture in visual-diff.test.mjs disagree, the fixture wins and the budget is renegotiated in the plan, never tuned here to make a run pass.",
    "widths": "Capture widths in CSS pixels. 1440 = desktop, 390 = iPhone-class mobile. An empty list is a hard error: a run that captures nothing cannot pass.",
    "maxLayoutShiftPx": "Budget from the redesign acceptance criterion. Gated against layoutShiftMaxPx -- the MAXIMUM observed row displacement, not the p99. A 6px sitewide button move touches few enough rows that a percentile hides it, and hiding it is exactly the defect this harness was repaired for.",
    "minShiftCoverage": "Fraction of structured baseline rows that must be found again in the candidate. This is what catches the two failures the pixel and p99 metrics are blind to: content that moved further than diff.mjs's 240px search window, and a page whose content changed outright. Both report a shift of zero; both drop coverage to near zero.",
    "maxChangedPct": "Percentage of the page that may look different before it is treated as a regression. Cosmetic anti-aliasing noise sits well under 0.5%.",

    "maxScaleDivergence": "How far the two capture widths of ONE page may disagree about that page's affine SCALE. Under the affine model this is the sharper of the two divergence checks: both widths render from the same stylesheet, so a leading change should compress them by a comparable ratio even though their layouts differ. 0.05 is a first calibration and should be revisited once real per-page scales from a leading change have been seen; tighten it, do not loosen it, without saying why.",

    "minFitConfidence": "Fraction of a page's structured rows the affine fit must explain before the fit counts as a fit at all. Below this the run FAILS with an explicit model-does-not-fit reason rather than reporting residuals against a fit that does not hold -- if the model is wrong, every shift number beside it is measured against nothing. Deliberately NOT waivable (see UNWAIVABLE in lib/gate.mjs) and NOT overridable per page: a page the instrument cannot describe is a human conversation, not a config line. 0.75 separates the observed cases by a wide margin -- a real leading change fits at 0.958, a constant-offset model on the same page fitted at 0.034 -- but it is a first calibration, not a measured threshold.",

    "maxOffsetDivergencePx": "How far the two capture widths of ONE page may disagree about that page's estimated global vertical offset. Deliberately generous: the 1440px and 390px layouts are different documents and a leading change legitimately compresses them by different amounts, so this is a 'wildly differently' detector rather than a precision gate. The sharp signal alongside it needs no budget at all -- if one width GREW while the other SHRANK, that is reported regardless of magnitude, because no single spacing change does that. 400 is a first calibration and should be revisited once real per-page offsets from a leading change have been seen; tighten it, do not loosen it, without saying why.",
    "expectedToChange": "Per-metric waivers, not a blanket pass. Shape: { \"route\": \"/about/\", \"reason\": \"new hero type scale\", \"waive\": [\"changedPct\"] }. Waivable: changedPct, heightDelta, structural. NEVER waivable: layoutShiftMaxPx, shiftCoverage -- a config that tries is a hard error. A page may waive changedPct and remain shift-gated; that is the whole point of the restructure. Bare route strings are rejected so an old-shape config cannot silently keep its old blanket-waiver meaning.",
    "pageOverrides": "Moves ONE named budget on ONE named page, in the direction that metric's TYPE allows, for a stated reason, until a stated date. Shape: { \"page\": \"/about/\", \"metric\": \"maxLayoutShiftPx\", \"value\": 24, \"reason\": \"new hero type scale moves the fold ~20px, approved in the Wave A design review\", \"expires\": \"2026-09-30\" }. CEILINGS (maxLayoutShiftPx, maxChangedPct) may only be RAISED, capped at 100x the global -- loud is fine, infinite is not, so value: 999999 is rejected. The FLOOR (minShiftCoverage) may only be LOWERED, bounded to [0.75, 1) -- lowering is the shippable direction for a deliberate restyle, and below 0.75 a quarter of the page's rows could not be matched at all, which is a human conversation rather than a config line. An override can NEVER disable a metric and can never rescue a page whose shift could not be measured. Rejected on load: unknown metric, wrong direction, out of bounds, missing or empty reason, missing or malformed expiry, an expiry more than 90 days out, a duplicate page/metric pair. An EXPIRED override fails the run loudly rather than lapsing back to the global budget, so a temporary allowance cannot quietly become permanent. Overrides affect only the page and metric they name. This exists so a deliberate restyle is shippable under review; the global budgets above stay unwaivable and are not to be lowered to make a run pass.",
    "expectedRedirects": "Client-side redirects the capture is expected to observe, as route -> settled destination. A redirect that is not declared, points somewhere else, or stops happening fails the run. Without this the harness screenshots the destination page under the source route's name and reports it as unchanged. /gallery/ and /process/ genuinely redirect today (verified 2026-07-30)."
  }
}