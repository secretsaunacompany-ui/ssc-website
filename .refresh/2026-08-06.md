# Code Refresh: ssc-website — 2026-08-06

## Baseline
14 suites green (~600 assertions), build clean, at origin/main tip 7b506b5
→ **17 suites green** on branch `refresh/2026-08-06` (three new: `jsonld:test`,
`functions-gate:test`, `lightbox:test`). Counts at tip: quote-funnel 89, events 76,
rhythm 77, fonts 31, visual-diff selftest 164, dom-integrity selftest 114,
lint:css-gate 16, package-claim 8, prices-version 7, build-cache 8, cloudinary 16,
csp-hash 6, functions-gate 10, lightbox 7, jsonld 3, models-json round-trip clean.

Audit: 6 Fable subsystem readers (workflow wf_9a7afbc5-38c), 39 raw findings,
~988k reader tokens. Oracle: ROADMAP.md + docs/redesign-2026-07 authority table +
CLAUDE.md — **no ARCHITECTURE.md exists**, so architectural drift was judged
against the doc authorities and idiom rather than a written design (weaker;
creating one is a standing recommendation below).

## What changed

- `bb38a59` roadmap: restore the post-deploy record clobbered by the Aug-5 cherry-picks
- `1359e68` docs: supersession banners — doc 35 heater rows + audio sentence, doc 13 prices, doc 14 hero_hold boundary
- `b874d06` css: remove the embedded-booking system's dead styles and the pre-redesign residue
- `c2eb935` deps: uninstall postgres — imported by nothing
- `c015e00` css: advisor error text joins the --error token
- `952d8e0` csp: drop trust grants nothing uses — cdn.jsdelivr.net, script.google.com, X-XSS-Protection
- `127ee38` harness: name the concurrent-run hazard; modal: document the package-toggle sacrifice honestly
- `2a81126` plan: critic rounds 1-2 folded, D2 bundling probe recorded (zisi carries site.json)
- `9601948` plan: round-3 critic folds — schema-doc sweep completed, B4 capture/restore focus. Loop converged.
- `89c7217` ops console: fix the CSP-broken login, and make the whole attribute class impossible
- `689a69d` advisor: dark front-end now means dark back-end
- `245ed85` analytics: book_page_view stops racing the tracker it reports to
- `8074a6d` faq: the one schema block that escaped the jsonld sweep, and a guard for the class
- `f3a7b0d` contact form: green status stops proving anything, here too
- `42f8a7f` privacy: discoverable from every route, and the contact form says where the data goes
- `be0ff3c` copy+schema: the site stops claiming places and praise it does not have
- `4945a3c` leaflet: SRI on both CDN artifacts, and a failure that says so
- `add5c13` blog: delete the armed trap (Lee's ruling)
- `1058259` functions: Node 18 (EOL) -> 22
- `b40f57b` harness: expectedToChange joins the expiry discipline — the last silent-forever waiver class closes
- `55719bf` harness: the three dist-trusting suites stop certifying stale builds
- `094dd84` harness: five private copies of the scratch machinery become one, derived from the source of truth
- `ae992bb` lightbox: held to the bar the funnel modal set — the reachable half
- `934af4a` booking-admin: one rate limiter, the platform's own IP header
- `285349b` type: the uppercase tracking outlier joins --tracking-caps; four survivors become named exceptions
- `0dd1540` lightbox: focus must not strand on the button we just hid
- `59b8a12` certify the refresh: DOM declarations for the whole range, and the notes the gate asked for
- `1692d60` pixel: declare the refresh's intended visual deltas, and record the three residuals it refuses

## What was deliberately left (the "no" list)

1. **Package-toggle wipes a pre-package à-la-carte pick** (js/modal.js wasOn
   branch). A visitor who chooses Cedar a-la-carte, tries the Premium package,
   then unchecks it, loses the Cedar pick to the group default. Money is never
   wrong — the reset lands on $0, summary always matches total — so the damage
   is one visible lost click. Remembering per-slot pre-package state is more
   machinery than that justifies. The code comment now documents the sacrificed
   ordering honestly instead of denying it exists.

2. **Analytics tracker's defer ordering couples render boot to a third-party
   download** (scripts.njk + init.js). A stalled ssc-ops response delays
   hero/nav visibility site-wide — contradicting analytics.js's own "must never
   break the funnel" doctrine. BUT the ordering is load-bearing: WP-0a's
   double-count suppression requires init.js's submit listener to register
   before the tracker's (registration order = dispatch order). The naive fix
   (async) resurrects the double-count. A real fix (dynamic injection after
   listener bind, or moving the boot off DOMContentLoaded) is a design task for
   a Wave B batch, not a cleanup. Left alone, hazard documented here.

3. **map.js inline style literals** (#c4a57b ×8 = --ember by coincidence of
   value, off-system #4A90E2, 'Outfit' by name). Genuine token drift waiting to
   happen, but the popups render in Leaflet's injected DOM where a class-based
   restyle is a real visual-change risk, the content is static first-party, and
   no suite watches open popups. Worth a small deliberate batch (move to
   .map-pin classes) when /locations/ is next touched — not a drive-by.

4. **Gallery hover/overlay CSS duplicated** between .gallery-mosaic scoped and
   bare .gallery-item rules (~30 identical lines). Both families live; the
   scoped copies merely re-assert at higher specificity. Removable, but the
   harness ceremony to certify the merge exceeds the value of 30 stable lines.
   Counterweight recorded: the model-index vs compare-ledger subgrid twin is
   DELIBERATE ("the two ledgers should read as the same instrument") — leave it.

5. **sauna.njk's static heater markup shows S6/S8 values under a comment
   claiming it is the S4 default** (latent only — handleHeaterOptions() rewrites
   before paint; modal unreachable without JS). Fixing it moves the price-table
   hash → prices-version ceremony + dom-integrity certificate. DEFERRED to the
   next pricing-window batch per the hands-off-pricing-files constraint. The
   next pricing session should carry it.

6. **Verified Chesterton fences, explicitly not "fixed":** `rm -rf dist` in the
   build command (kills the Netlify publish-dir resurrection bug, 2026-07-28);
   booking-ops.html/js passthrough (ops console is meant to be served;
   OPS_ADMIN_TOKEN fails closed — verified 401 live); style-src 'unsafe-inline'
   (required by booking-ops inline styles + page background-images; tightening
   is a project); the non-constant-time token compare in booking-admin
   (theoretically timing-unsafe, practically irrelevant over serverless jitter
   for a 64-hex token); the models-json selftest's in-place mutation (defended
   three ways, restore verified byte-for-byte — its scary shape is its point);
   the 188-entry dom-integrity whitelist with no expiry (range-scoping is
   STRICTLY STRONGER than calendar expiry: entries are inert outside their
   exact baseline..candidate pair and unconsumed in-scope entries fail the
   run — it is an attribution ledger, not debt); capture.mjs's Range-header
   cache bug documentation (matches the code exactly; video is stubbed before
   the cached-host rule so determinism provably doesn't rest on the buggy
   path — the ROADMAP deferred entry is accurate as written, nothing owed).

7. **The five suites' per-suite mutate() helpers were NOT unified** in the B3
   consolidation: their single-occurrence-anchor error messages are
   suite-specific and load-bearing.

8. **connect-src unpkg.com kept** in the CSP trim despite Leaflet loading via
   script/link tags (not fetch) — cutting it is safe only after a draft-deploy
   look at the live map, which belongs to the next deploy.

## Bugs routed out (bucket D — fixed via the critic-gated plan, not folded silently)

Ten live defects, each its own commit with its own test net:

1. **Ops console login CSP-broken** — an inline `onsubmit` attribute the production
   policy refused (hashes cover `<script>` elements; attributes need `unsafe-hashes`,
   deliberately not granted). The handler never ran, the form default-submitted, the
   overlay looped forever. Lee could not have reached residual reservations. The
   csp-hash suite gained the attribute sweep that makes the whole class impossible.
2. **Advisor endpoint publicly POST-able while dark** — an unauthenticated LLM proxy
   whose only guards were a per-instance in-memory limiter and CORS. Now gated on the
   same feature flag as its UI; bundling PROBED (zisi carries site.json), not assumed.
3. **`book_page_view` dropped on every production view of `/book/`** — it ran at
   analytics.js evaluation time, racing the deferred tracker it reports to. The suite
   had been certifying an ordering production never has.
4. **FAQ structured data entity-garbled** — the one schema block that missed the
   `jsonld` filter sweep, feeding Google `What&#39;s the difference…`.
5. **Contact form trusted `response.ok` alone** — a 2xx with no `next` fired success,
   reset the form and navigated away while the message evaporated: the exact shape
   proven live 2026-07-30. Failures were a blocking `alert()`, invisible to analytics.
6. **Privacy policy undiscoverable from 18 of 19 routes**, and the contact form
   collected name/email/phone for a US endpoint with no notice at all.
7. **Live false claims** — the homepage advertised booking at "our Aldergrove
   location"; `/locations/` meta promised venues the page does not show.
8. **Self-serving review markup + phantom SearchAction** — manual-action risk under
   Google's review-snippet policy; a declared `?q=` endpoint no page can serve.
9. **Leaflet loaded with no SRI** under a CSP that grants unpkg script execution —
   the polyfill.io class, on a pinned version whose hashes are trivial.
10. **Node 18 (EOL) function runtime** — rot with a deadline that would have fired
    during an unrelated deploy.

**Plus one defect this refresh introduced and its own new suite caught:** B4's
focus-restore used `document.body.focus()`, which is a no-op — so "restoring" focus
stranded it on the close button just hidden, strictly worse than before. Fixed in
`0dd1540`. Recorded because a refresh that hides its own mistakes is not an audit.

## Recommendations beyond this skill's scope
- **Write an ARCHITECTURE.md.** The audit ran degraded without one. The repo
  has exceptional per-decision documentation (comments, config prose, docs/)
  but no single written design to reconcile against.
- **Progress Way schema address + opening hours** (head.njk LocalBusiness):
  still publishing the closed shop's address and Mo-Fr hours. Blocked on
  doc 13 NEEDS-LEE item 7 (what address/hours story replaces it). Lee decision.
- **F1 advisor-prompt hazard stands** (restored to ROADMAP): all four
  netlify/functions/prompts/*.js files must be class-swept before
  features.advisor ever flips — inquiry.js tells SC prospects wood-fired is
  included when SC wood = IKI +$6,500.
- **Unset ANTHROPIC_API_KEY in the Netlify env while advisor is dark** (belt
  alongside the D2 code gate). Lee dashboard action, one minute.
- **npm-audit backlog** unchanged (ROADMAP deferred; needs its own plan).
- **secretsaunacompany.com** typo-domain question unchanged (parked ad-lander).

## What to watch

- **Three refused pixel residuals**, recorded not waived (the shift gates are
  unwaivable by design): `/contact/` at both widths returns MODEL-DOES-NOT-FIT
  (affine explains 62.2%/53.5% of rows — inserting two blocks mid-page is not a
  single scale-and-offset, and the harness says so itself); `/` @390 reports a
  layoutShiftMaxPx that **moved between runs on identical commits**, 24px then 73px.
  That instability is the finding — the known row-matcher tolerance issue, now with
  a second witness. The full-range run therefore exits non-zero, exactly as Wave B-1
  did with its own recorded reds: the certification is the written record, not a
  green exit.
- **The DOM run's JS RUN FAILURE is expected and waived nowhere** — the batch edits
  four scripts by necessity, and this instrument certifies markup with scripts
  disabled. What the scripts do is certified by quote-funnel 89, events 76, lightbox
  7, functions-gate 10. Read the two together.
- **One transient events:test failure** in ~6 runs, not reproduced in the 4 runs
  after (76/76 each). Matches the existing FLAKE WATCH ledger entry; not introduced
  here, not cleared either.
- **`expectedToChange` waivers now expire.** A lapsed stamp fails the ENTIRE
  visual-diff suite at `loadConfig`, by design. **Re-stamp or retire all 18 entries
  by 2026-10-05**, or at the deploy baseline advance — whichever comes first.
- **Thin coverage that remains:** the `/locations/` Leaflet map has no suite (the
  SRI change was verified by hand, including a tamper test); the gallery grid has no
  keyboard path to open the lightbox at all (named Wave B residual, Jen's lane).
- **Deploy-time checklist** (production verification these changes could not do
  locally): one `/ops` auth round-trip and an advisor 503 on the draft URL under the
  new Node 22 runtime; a glance at the `/locations/` map.

## Drift vs the oracle
- **The ROADMAP itself was the biggest drift item found:** the Aug-5 doc
  cherry-picks regressed it wholesale to pre-deploy state (status, shipped
  ledger, 25-entry deferred list, F1 hazard — 214 lines → 10). Restored in
  bucket A from d0397b7 + the two Aug-3 gate texts. Root cause: twin commits
  cut from a pre-deploy session branch carried the whole stale file, not a
  delta. The project-onboarding rule ("ROADMAP is canonical") makes this class
  of regression maximally damaging — every onboarding since Aug 5 read false
  state.
- **Doc 35 / doc 13 price supersession** was silent (no banner pointing at
  doc 41 / pricesVersion 4); canonical models.json even warns against the
  exact "correction" a doc-35 reader would make. Banners added in bucket A.
- **Code matched its stated intent remarkably well everywhere else.** The
  funnel money path reconciled line-for-line with doc 35 as amended (all 19
  rows, three independent derivations). The clean_areas lists from all six
  readers are in the workflow output.
