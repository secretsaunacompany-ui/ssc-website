---
status: live
current: "DEPLOYED 2026-08-03 (PR #2, merge d2661b1, branch tip fb33e70): Wave B-1 + base-price reprice + complete-sauna catalogue (pricesVersion 4) shipped as ONE train — hero reveal, container-padding fix, /saunas/ ledger, S2 $23,500 / S4 $29,500, standard electric-or-wood heater included (wood on S4/S6/S8 per Lee field ruling), IKI premium tier at the 30% stove convention, mounting group with $3,500 trailer integration (trailer client-supplied, sized by final box length), delivery removed from quoting, S6/S8 cost-basis un-held, SC held behind the photography gate (derived from-$71,500 ready). Production-verified in browser post-deploy incl. a live $34,500 probe configuration; receipt gate CLOSED per Lee ruling 2026-08-03 (genuine inquiries prove delivery; the synthetic-sender probe was discarded, not filtered). Rollback disarmed. STANDING TAILS: F1 — netlify/functions/prompts/inquiry.js:18 (+ product.js:29 same class) tells SC prospects wood-fired is included when SC wood = IKI +$6,500; DARK behind features.advisor:false; MUST class-sweep all four prompt files before that flag ever flips. First manual funnel check due 2026-08-07. Formspree dashboard items (allowed-domains, 90d auto-delete, tier) still Lee's. The two-deploy split and its receipt protocol in deploy_gates are SUPERSEDED by the single-train deploy — kept below as historical record."
next: "WAVE B — Lee's post-deploy feedback leads (2026-08-01, fixed decisions in doc 00 addendum): (1) restore the hero-first reveal on the homepage (image arrives clean and unobstructed, beat of time, THEN nav + title — the Wave A motion cleanup deleted an effect Lee had explicitly asked to keep; capture failure, now recorded); (2) /saunas/ leads with the model index, comparisons demoted below (confirms doc 10 §3.2 verbatim — live page still leads with comparisons); (3) spacing/container rhythm is a first-class WP-2 acceptance criterion. Then the queued candidates: mobile-gutter batch + ember-dark decision; modal-scoping batch (dom-integrity cap raise); WP-2 composition / WP-3 copy / WP-4 pages (unblocked by Lee's remaining fact answers) — WP-2 now carries five named inbound items from the Wave B-1 gates, recorded here so they are not rediscovered: (i) Jen's §3.14 ADJACENCY COUNT is under by FOUR; she will amend her spec at her next touch, so until then read the spec's count as low by four rather than trusting it; (ii) C-2, compare-table typography; (iii) C-3, centered thresholds; (iv) B4's N7 information-architecture dead-end — home's 'Explore custom builds' and 'View mobile options' both point at /saunas/, whose Commercial & Custom section B4 removed, so they now land on a page with no custom or mobile content; the honest destinations are /commercial/ (WP-4, does not exist) or /contact/, and it is a copy/IA decision for Lee rather than a patch; (v) the tier-fidelity manifest from the post-wave harness index, which wants pairing with WP-2 because both need a human-maintained per-route composition record and building two of those separately would be the waste; case studies (parked pending client agreements); npm-audit remediation; ssc-ops tracker root-cause fix (cross-repo). Hand-off: ~/marvin/state/ssc-website-handoff-2026-08-01.md. PACKAGE SWAP-AT-DELTA CONFIGURATOR (deferred 2026-08-03, out of scope for the D4 fix that introduced the rule): Lee ruled 2026-08-03 that a package OCCUPIES each select-one group its includes touch, and choosing a different option in an occupied group bills at max(0, option - baseline) as an upgrade line, never both full prices and never below zero. The quote generator IMPLEMENTS this (skills/pdf/generate-document.js resolveSelection(), canonical packages.premium_finish.upgradableGroups). The modal does NOT: with the package on, exterior and interior are locked to the package's included cedar / clear cedar and shown as included at $0, which is honest but not yet swappable. Audio is already open, because it is the additive exception. The work is a configurator that offers the non-baseline options in an occupied group at their delta price, with the label saying what they substituted for -- and it needs the delta arithmetic in js/modal.js plus per-model delta spans in the markup, because the delta varies by model exactly as the option prices do. Until it ships, a customer who wants yakisugi WITH the package cannot configure it on the site and has to be quoted by hand -- the generator gets it right, the page cannot express it."
deploy_gates:
  - "GATE STATUS UPDATE (Lee, 2026-08-03): the Formspree receipt test is WAIVED — Lee is satisfied by the resolved root cause (his tests were caught by Formspree's own server-side spam filter; real inquiries deliver, embr 08-02 proves it). The three pre-deploy dashboard items are DOCKED, not blocking: (1) 90-day submission auto-delete (Lee unconcerned), (2) plan tier confirmation (it's the free tier — formspreeAutoresponse stays off), (3) privacy-page update + Petra review. NEW REQUIREMENT for the pricing arc (in flight in Lee's other window — this session keeps hands off pricing files): the site must AUTOMATICALLY sync with the newest canonical pricesVersion rather than relying on the manual bump-both-together discipline — e.g. a build-time step that derives data.js prices from canonical models.json so drift becomes impossible. Deploy trigger belongs to Lee / the pricing window."
  - "HARD GATE — Formspree receipt test from the Netlify draft-deploy URL: one submission with realistic content through the real modal MUST arrive in the inbox before ANY production deploy of the quote funnel. Formspree returns HTTP 200 success on silently-discarded submissions (proven 2026-07-30: localhost test accepted with documented success body, never delivered, Lee confirmed absent from all inboxes incl. spam) — a green status code proves nothing; only the inbox does. ROOT CAUSE RESOLVED (Lee, 2026-08-03): Formspree's OWN server-side spam filtering was catching the test submissions — delivery works for real inquiries (embr inquiry arrived normally 08-02). Gate procedure accordingly: submit REALISTIC content from the deployed draft URL (localhost origin + test-shaped content is exactly what their classifier eats), and if a test doesn't arrive, check the Formspree dashboard's spam view BEFORE diagnosing delivery. Production implication: a terse real inquiry could be spam-caught just as silently — the WP-0a weekly zero-submission Telegram check stays load-bearing, and the dashboard spam view deserves a periodic glance. Companion: the WP-0a weekly zero-submission Telegram check is the production-side alarm for the same failure mode."
  - "Lee dashboard actions before funnel production deploy: (1) Formspree submission auto-delete set to 90 days (PIPA retention, Petra item 3); (2) confirm Formspree plan tier (gates whether formspreeAutoresponse can ever be enabled); (3) privacy page updated for the five quote fields + US processor + both retention stories, then Petra review."
  - "PRICE-SHEET PARITY: models.json v2 (pricesVersion 2) must be live in ~/marvin/content/reference/operations/ BEFORE the next quote is issued — Lee's firm-prices ruling (2026-07-30) is already effective for new inquiries, and the v1 sheet under-quotes an S4+changing-room+interior by $8,600 vs the repriced site. v2 sits in the session worktree (byte-identical to docs/redesign-2026-07/41-models-v2.json); lands in the primary at the session's /save merge — verify at deploy AND at next quote, whichever comes first. The site round-trip script gains a canonical-path byte-identity assertion so the next repricing cannot pass by validating only its own copy. LANDED 2026-07-31 (83421b9). RESOLVED — the canonical file reached ~/marvin and `npm run models-json:roundtrip` was RUN on 2026-08-01: ROUND-TRIP CLEAN, exit 0, byte-identity PASS at 9644 bytes. The earlier 'correctly red until the MARVIN merge lands' status no longer describes reality. Note the primary ~/marvin checkout runs a non-main branch (see reference_monitor_deploy pattern), so a FUTURE repricing's merge must again reach the branch the primary actually has checked out, not just main. Path overridable via SSC_MODELS_JSON."
deferred:
  - "CODE REFRESH 2026-08-06 (branch refresh/2026-08-06, 28 commits, UNMERGED — Lee's call). Whole-repo audit by 6 Fable readers against the doc authorities; plan gated by THREE critic rounds to convergence; Stage 6 equivalence review PASS WITH NOTES, its two pre-merge notes closed. Suites 14 -> 17 (new: jsonld:test, functions-gate:test, lightbox:test). TEN LIVE DEFECTS FIXED, biggest first: the ops console login was CSP-broken in production since the policy existed (inline onsubmit refused, form default-submitting, login looping — the console was unusable and residual reservations unreachable); the contact form trusted response.ok alone and would fire success + navigate away on a silently-discarded 2xx, the exact shape proven live 2026-07-30; the advisor function sat publicly POST-able as an unauthenticated LLM proxy while its UI was flagged off; book_page_view was dropped on EVERY production view of /book/ (it raced the deferred tracker it reports to, and the suite had been certifying an ordering production never has); the FAQ JSON-LD reached Google entity-garbled; /privacy/ was undiscoverable from 18 of 19 routes and the contact form collected PII with no notice; the homepage advertised booking at Aldergrove; self-serving review markup carried manual-action risk; Leaflet loaded with no SRI under a CSP granting unpkg script execution; Node 18 (EOL) runtime. ALSO: the ROADMAP itself was the single biggest finding — the Aug-5 doc cherry-picks had regressed it wholesale to pre-deploy state (214 lines -> 10), taking the shipped record, the deferred ledger and the F1 hazard note with them; restored in this branch. Harness gained: expectedToChange expiries (the last silent-forever waiver class), a stale-dist guard on the three dist-trusting suites, an inline-event-attribute CSP sweep, and five drifted scratch-helper copies consolidated to one. DECLINED-IN-WRITING list of 8 in .refresh/2026-08-06.md — read it before re-finding them. DEPLOY-TIME CHECKLIST: /ops auth round-trip + advisor 503 on the draft URL under Node 22; UNSET ANTHROPIC_API_KEY in the Netlify env while features.advisor stays false — the D2 code gate makes the endpoint answer 503, but the key sitting in the env is a standing spend risk the moment any future edit reorders that gate, and pulling it costs a minute (Lee dashboard action); a look at the /locations/ map (SRI, hand-verified incl. a tamper test); RE-STAMP OR RETIRE the 18 expectedToChange entries by 2026-10-05 or visual-diff fails closed at loadConfig. THREE REFUSED PIXEL RESIDUALS recorded not waived: /contact/ affine model-does-not-fit at both widths, and / @390 whose layoutShiftMaxPx moved 24px->73px BETWEEN RUNS on identical commits — the known row-matcher tolerance issue with a second witness."
  - "npm-audit remediation: 70 pre-existing vulnerabilities surfaced when the lockfile was committed (7 critical, 35 high; 1 high in the production tree — ws via @supabase/supabase-js). Needs its own plan; npm audit fix --force on a live site is not a relay side-quest. Recorded 2026-07-30, Wave A batch 1."
  - "Site CLS: lazy images without width/height/aspect-ratio — real cumulative layout shift for visitors, found during harness repair. Recorded 2026-07-30; SCOPE RE-MEASURED 2026-08-01 (Wave B-1 B0) and the original '/about/ and elsewhere' no longer describes reality. /about/ is CLEAN — 6 of 6 lazy images carry width and height. Swept every built page (parse each dist/**/*.html, take every img with loading=lazy, flag any lacking BOTH width and height and lacking aspect-ratio): 63 lazy images, 5 offenders, ONE per page, on exactly the five service-area pages — /north-shore/, /sea-to-sky/, /squamish/, /vancouver/, /whistler/. All five are the same template line, src/_includes/pages/service-area.njk:15 (the Cloudinary intro image), so this is a one-line fix at one source site, not a sweep. Candidate WP-1b line."
  - "Media cache keys on URL alone, ignores Range header — ranged media replays wrong (hero video). Site bug documented in scripts/lib/capture.mjs. Recorded 2026-07-30."
  - "Badge SVG mask rendering verified in Chrome only — one Safari look owed before the footer seal ships. Recorded 2026-07-30."
  - "CROSS-REPO: ssc-ops shared tracker's form_submit handler reads a field named 'sauna' that no form has — every site on that tracker records interest 'not specified'. This site now suppresses the generic listener and sends its own event (WP-0a seam), but the tracker itself needs its own authorized change in the ssc-ops repo. Recorded 2026-07-30."
  - "CROSS-REPO, PAIRED (Petra 2026-07-31, HIGH not deploy-blocking): ssc-ops tracker PIPA s.35 work — (a) minimize IP at ingest (truncate last octet or salted hash; pageview counts don't need full IPs; takes the store out of PI territory) + set a retention window on page_views/sessions; (b) then add one analytics-retention sentence to the site's privacy page (its FLAG 2 openness gap). Note: the schema's country/city columns are DEAD — verified never populated by the ingest (FLAG 1 cleared 2026-07-31); if geolocation is ever added, the privacy page's 'never your location' sentence must be re-scoped FIRST."
  - "ANALYTICS READING NOTE — hero_hold_* (rewritten 2026-08-01, Wave B-1 batch B1; supersedes the 2026-07-30 note and its 2026-08-01 stale marker). WHAT THE PAIR MEANS NOW. Two events, exactly one per homepage view, and the branch between them is decided by SETTLE_MS in js/animations.js — the choreography's own boundary, not a threshold invented for the metric. hero_hold_skipped: the first scroll arrived BEFORE the page finished settling. hero_hold_complete: the first scroll arrived after it, or the visitor left without scrolling. Both carry `ms`, the time the visitor actually gave the photograph, so the MEDIAN of either branch is usable on its own — that is the change from the old note, which had to warn that only the skipped branch carried real attention time because complete always fired from a 5s auto-reveal timer (deleted in 156c073). No timer reports this now: `ms` is measured to the moment the visitor acted or the page went away (scroll, visibilitychange, or pagehide — two listeners, latched, so a navigation away still reports exactly once). WHERE `ms` IS MEASURED FROM (corrected 2026-08-01, Razor WARNING-1, before any data was collected): the CHOREOGRAPHY's clock — the instant `.reveal-ready` lands on `<html>` — read from SSC.reveal.startedAt. PRECISION, added 2026-08-02 (B6, Razor B1 residual): the earlier wording here said 'the instant .reveal-ready lands AND the held transition-delays begin counting', which conflated two instants that are ONE FRAME apart. The class landing and the start of the delays are not simultaneous — delays start from the style recalculation the class change provokes, which the browser performs on the next frame, about one frame later (one 60Hz frame is 16.7ms; the gap measured at ~19ms, i.e. about one frame). So `ms` is anchored ~19ms BEFORE the choreography actually starts spending and over-credits every visitor by up to one frame. This is recorded, not corrected, and the size is the point: one frame against a 1600ms hold is ~1.2%, against the 53-168ms (and unbounded in a rAF-starved tab) error of the DOMContentLoaded anchor it replaced. It is bounded and always in the same direction, so it cannot flip a branch that was not already within a frame of the boundary — and the events suite's two-frame agreement assertion is what would notice the gap growing. Do not quote hero_hold medians to a precision finer than one frame. It was previously DOMContentLoaded, which is 53-168ms earlier because init.js waits out a double rAF before enabling transitions, so every view was credited with time the choreography had not begun to spend and a visitor who watched the WHOLE arrival and scrolled at the end of it could be branded `skipped`. The events suite asserts the two clocks agree within two frames. WHO IS NOT COUNTED: reduced-motion visitors emit NOTHING. They are never held — both the CSS mirror and the JS guard compose the page instantly — so they would have landed in `skipped` essentially always, and since the ratio below is the retune instrument, each of those views was a vote to shorten a beat that visitor never saw. Read the ratio as a statement about visitors who were actually held; it is silent about everyone else, deliberately. THE BOUNDARY: SETTLE_MS = 2720ms = --hero-hold 1600 + --transition-reveal 1000 + 1 x --stagger-step 120 (the last held member, .hero-content at --i:1). It was 1240 before B1 and it was also 120ms miscalibrated before B1, because its derivation comment assumed a last member at --i:2 (a scroll cue that does not exist). Any hero_hold reading taken before the cutover was branched at a different, wrong boundary and is NOT comparable to readings after it. The events suite's agreement fixture asserts the boundary against the page the browser actually resolved, so the number in this note cannot drift from it. MECHANISM CHANGE, 2026-08-02 (hero graceful-cancel, B7 rides): the hold is no longer a `transition-delay` — RevealManager withholds `.seen` from the held pair until `startedAt + HERO_HOLD_MS`, where HERO_HOLD_MS is READ from the `--hero-hold` token at init. SETTLE_MS and its arithmetic are UNCHANGED (`.seen` used to land at t=0 with the transition waiting out the delay; it now lands at t=1600 with the transition starting at once — same visible timeline), and so is everything else in this note, including what `ms` is measured from. What changed is the agreement fixture's three terms: it now asserts SSC.reveal.holdMs === the computed `--hero-hold`, that the held pair's computed `transition-delay` is its ORDINARY stagger and nothing more, and that hold + delay + duration === SETTLE_MS. WHAT 'COMPLETE' MEANS IN PLAIN WORDS: the visitor stayed for the WHOLE arrival — the 1600ms the photograph was held alone plus the ~1.1s the nav and title took to arrive over it. It is a stronger claim than the old 'complete', which merely meant a 5s timer had fired. WHAT TO DO WITH IT: the hero_hold_skipped:complete RATIO is the retune instrument for --hero-hold, per doc 21 N2's own pre-authorisation ('retune with data, not taste'). Read it after two weeks of POST-CUTOVER data only. 1600ms was chosen at the patient end of the band on purpose; a high skip ratio is the signal to shorten it. RETUNE SITES, named exhaustively so the promise that follows is checkable — and COMPLETED 2026-08-02 (B6, Razor B1 residual), because the earlier list named three sites and then claimed they were the only ones, which was false: (1) `--hero-hold` in styles.css; (2) `SETTLE_MS` in js/animations.js; (3) `SETTLE_MS`/`PAST_SETTLE_MS` in scripts/events.test.mjs; (4) the M9 mutation's SEARCH STRING in scripts/events.test.mjs, which hard-codes the literal `--hero-hold: 1600ms;`; (5) the M9 detector's own comparison in the same fixture, which hard-codes `=== 2720` inside a `page.evaluate` because browser-context code cannot close over the Node-side constant. The old sentence — 'the agreement fixture derives everything else from the computed style rather than hard-coding a copy' — is true of the AGREEMENT fixture and untrue of M9, which carries two hard-coded copies of numbers that agreement derives. HOW EACH FAILS, because they are not equally loud and the difference is the whole reason to name them: sites 1-3 and site 4 fail LOUDLY — the agreement fixture recomputes delay+duration from the browser and compares it to `SETTLE_MS`, and `mutate()` throws by construction when its search string does not match exactly once ('expected exactly one occurrence ... If the code was refactored, update the mutation. Do NOT delete it'). Site 5 is the quiet one and is the reason this list needed completing: if a retune updates M9's search string but not its `=== 2720`, the detector's comparison can never hold, the mutation reports 'detected' unconditionally, and M9 goes green while proving nothing. That is a fixture passing vacuously, which is the exact failure class the Y13 vacuity guard was added for elsewhere. Retune all five, and re-read this note's SETTLE_MS arithmetic when you do. Note the escape hatch when reading: a scroll, keypress or tap during the beat cancels the hold instantly, so 'skipped' means the visitor chose to move on, never that they were made to wait. CUTOVER: Wave B-1 Deploy 1, date TBD at merge — the merge commit fills this in. Data before that date is branched at 1240 and must not be pooled with data after it."
  - "F-1 CONTAINER PADDING (found 2026-08-01, root cause of Lee's spacing finding; full record in docs/redesign-2026-07/21-resolved-tokens.md §6.2a amendment). `.container` declares `padding: 0 var(--gutter)` (styles.css:513-517) — a SHORTHAND, so it resets the block axis — at specificity (0,1,0), beating `section`'s tier rule (styles.css:529-531) at (0,0,1). Source-class census: 68 sections, 44 plain `.container`, 2 `.wide-container`, 22 non-container (the 68 includes 2 blog sections that never render — no blog posts are built). Re-measured 2026-08-01 by computed style (built dist served over HTTP, all 19 pages at 390px with JS disabled, getComputedStyle on every section) after a first pass got two numbers wrong by deriving them instead of rendering them: **40 of 68 sections carry ZERO vertical padding on BOTH edges, and 4 more are zero on the bottom edge only** — the `container section--pt-6` sites, where `.section--pt-6 { padding-top: 6rem }` (styles.css:2410) at (0,1,0) wins the top edge and renders pt=96px/pb=0px. `.section--pt-6` exists only to hand-patch F-1 at those 4 sites. Corollary: the mobile 5% override (styles.css:3187-3189) is INERT for all 44 containers (media queries add no specificity) and **reaches 8 sections, not the 22 first claimed** — the other 14 non-containers are inert for the same specificity reason (13 `.page-hero` at styles.css:729-730 and 1 `.section--no-padding` at styles.css:2414, both (0,1,0)); the 8 it does reach are 6 `.surface--elevated.hero-overlay`, 1 `.section--fade-edges`, 1 `.hero`, all measured at 64px/64px. It never flattened what §6.2a blamed it for. FIX SCHEDULED, not deferred: Wave B-1 batch B2 changes `padding` to `padding-inline` AND deletes the `.section--pt-6` rule (styles.css:2410-2412) in the same batch — without that deletion the 4 sites render 6rem top against `--section-pad` bottom, asymmetric and worse than before; deleting a CSS rule keeps B2's certificate a ZERO-delta dom-integrity run, and the 4 dangling markup class attributes come out in B5."
  - "MODEL-FACT CENSUS + CAPACITY DRIFT (verified 2026-08-01; sweep lands in Wave B-1 batch B4, values NOT touched in B0). Model facts are written independently in six places: js/data.js, docs/redesign-2026-07/41-models-v2.json, the canonical ~/marvin/content/reference/operations/models.json, the model-card markup in src/_includes/pages/saunas.njk, that page's ItemList JSON-LD, and the site-wide OfferCatalog JSON-LD in src/_includes/head.njk. PRICES are machine-tied across all of them — models-json:roundtrip asserts canonical byte-identity plus per-model site parity, 156 assertions, green 2026-08-01. CAPACITY IS TIED BY NOTHING, and the SC Commercial figure has drifted at four sites, all live: src/_includes/pages/saunas.njk:269 ('10–14 Person', en dash), :265 (img alt, '10-14 person'), :84 (ItemList JSON-LD, '10-14+ person'), and src/_includes/head.njk:159 (OfferCatalog JSON-LD, '10-14 person' — this one emits on 16 of the 19 built pages, measured 2026-08-01, absent only from booking-ops.html, /gallery/ and /process/; the earlier 'EVERY page' was not counted). Canonical says '10-12+ people' (41-models-v2.json:47), and js/data.js:123 already agrees with canonical — so the modal shows the right figure while the card above it shows the wrong one, on the same page. Measured reach of the wrong number: **19 rendered instances across those 4 source sites** — 16 via head.njk plus 3 more on /saunas/ (which carries 4 in total, one of them its own head.njk instance). No suite covers capacity: B4 normalizes all four sites to one canonical string and adds the missing assertion. Lee's veto point per plan default 2: confirm 10-12+ is the true physical figure before it propagates. CENSUS CORRECTED 2026-08-02 (Wave B-1 B4 fix round, Razor W4): the drift was FIVE sites, not four. The fifth is src/_includes/pages/home.njk:31, 'From 2-person micro saunas to 14-person+ commercial units' -- a THIRD distinct wrong figure (14-person+, against the card's 10-14 and the ItemList's 10-14+). It was invisible to the original census because that sweep grepped for the string '10-14' and this copy never contained it: a census built from one spelling of a wrong number cannot find the others. Corrected to '12-person+' in the same round -- George's sentence shape untouched, only the number moved. FLAGGED TO LEE as a one-line override if canonical is the wrong side of this: the standing default is 'capacity corrects to canonical everywhere', and if the SC really does seat 14 then it is the canonical sheet and js/data.js that need changing, not five pieces of copy. All five sites now read from, or agree with, canonical."
  - "REFUSED RESIDUALS — three visual-diff results deliberately left RED as truth-tellers, do not chase them (`_notes.residualsNotDeclared` in scripts/visual-diff.config.json (cited by KEY, not line: B6's prune moved it from :283 to :57, and the key is the stable address); surfaced into the ROADMAP 2026-08-01 so a future batch does not read them as unfinished work). /faq/@390 coverage 0.627 is below the 0.75 absolute floor (C35 rewrote every question node; resolved as a human conversation — Jen's rendered review passed it); fit-confidence failures on /@390 (0.706) and /saunas/+/gallery/@390 (0.565) are UNWAIVABLE and non-overridable by design. Separately and by RULE: `/` carries NO shift override even though its 30px@1440 is fully attributed, because the F1/F1b calibration fixtures evaluate route '/' against the shipped config — a home-page shift override disarms the harness's own self-test, and this property has already caught bad overrides three times (WP-1a's 18, plus the reviewer's and the orchestrator's own entries on 2026-07-31). All of these retire on a baseline advance; none is an action item. COUNT UPDATED 2026-08-02 (B6 prune): the config held 18 expectedToChange entries and 15 pageOverrides when counted 2026-08-01; it now holds THREE expectedToChange entries and ZERO pageOverrides. The 15 pruned expectedToChange entries and all 15 pageOverrides described Wave A changes that the baseline now contains, so they were unconsumed — and an unconsumed waiver is not harmless, it silently covers the NEXT change to that page and metric. The three preserved entries are `/` (B1's LCP/sizes rewrite) and `/saunas/` + `/gallery/` (B4's recomposition); those describe changes NOT yet in the baseline and retire at the Deploy-1 advance with everything else above."
  - "MODAL SCOPING — SHIPPED 2026-08-01, Wave B-1 batch B3. The configurator modal is no longer emitted sitewide: src/_includes/footer.njk wraps the include in `{% if configurator %}` and only src/saunas.njk sets that flag, so exactly ONE built page carries `id=\"saunaModal\"` where 16 did. The other 15 had been shipping a second, inert <form> whose empty configuration/model/estimated_total hidden inputs sat beside their real contact form — a behavioural reviewer read the /contact/ one as a live field that had lost its data. Written and measured once before in WP-0b-i and REVERTED there rather than shipped, because certifying a 355-token deletion needed dom-integrity's MAX_SUBTREE_TOKENS raised from 64 and raising a cap to clear one's own batch is the tuning-until-green move that instrument exists to refuse. It ships here with the cap raise (64 -> 400) carried as its own separately reviewed subject, argued from the measurement and fixture-proven at BOTH bounds (Z18 under the cap certifies, Z19 over it still fails loudly with nothing consumed, Z20 pins the value, D-m16 kills it as a mutation). Permanence: quote-funnel:test now asserts the presence invariant in both directions over the built site — every page with an open-modal trigger ships the modal, AND every page shipping the modal has something that opens it. The second half is the durable one; it is what catches a future page setting `configurator: true` without triggers, or the guard silently reverting to sitewide, once dom-integrity's baseline has advanced past this batch."
  - "HERO/NAV COLLISION — CORRECTION OF THE RECORD + WRAP CENSUS (2026-08-02, deploy-1 fix round, Razor CRITICAL-2). The fix round originally reported that the h1-behind-nav geometry PRE-DATED the wave, on the evidence that builds of ae0f40f (wave A) and 5748de8 (draft) measured identically at 390 — h1 top 85.5, clearance -5.5. That measurement was real but it was taken with the WEBFONT RESOLVED, and the conclusion was generalised to a state it never covered. IT IS FALSE AT 390. Re-measured fonts-BLOCKED at 390: ae0f40f wraps to 4 lines and clears at -5.5; 5748de8 wraps to 5 and sits at -29.3. Font metrics are identical between the builds; what moved is the h1 box, 287px -> 278px, because the wave unified the gutter (5% -> 24px per side at 390) and narrowed phone text by 9px, which tipped the wrap. So the wave DID introduce the 390 regression the behavioural eval measured. The pre-existence claim remains TRUE at 360, where both builds are behind the nav regardless of font state (-28 fonts-loaded, -39.3 fonts-blocked on wave A). THE BOUNDED SWEEP the false claim had foreclosed was then run: both builds, fonts-blocked, widths 360/390/414/430, all 17 rendered routes (19 HTML pages less two meta-refresh redirect stubs), comparing per-element wrap counts for headings, CTAs and the hero subtitle. FIVE elements changed wrap count: /@390 h1 4->5 (the defect, now fixed, +32 clear); /@414 hero subtitle 3->4; /vancouver/@414 h2 1->2; /whistler/@360 h3 1->2; /whistler/@390 h3 1->2. The last four are benign extra lines in normal flow — each was checked for nav collision, x-overflow and clipping under an overflow:hidden ancestor, and none collides, overflows or clips. Whole-build scan for the same three defect classes: wave A had 2 (the h1 at 360 and 390), the fix-round tip has ZERO. Nothing was changed beyond the h1, per the review instruction to fix only what collides."
  - "HARNESS, POST-WAVE — B5 FIX-ROUND RECORDS (Razor W-2, N-2, N-3, N-4; recorded 2026-08-02). (W-2, THE REAL GAP, no code yet) NO GATE ASSERTS JEN'S ASSIGNMENT. B5 wired 26 sections to tier classes from Jen Stage 0.7 §3, and the instruments certify everything EXCEPT the thing the batch was for: rhythm R2c/R2d prove that whatever wears `.section--tight` computes the tight token, dom-integrity proves the class token arrived, visual-diff proves pixels moved. Not one of them knows that home :14 was SUPPOSED to be tight and warranty :45 was supposed to stay standard. Swap two assignments between pages and every gate stays green. THE IDEA: a tier-fidelity gate reading a checked-in per-route manifest (route -> ordered list of section identifier + expected tier) and asserting the RENDERED sections match it in order, so the spec becomes executable and a future edit that silently re-tiers a section fails. Cost is the manifest needing a human to keep it true, which is exactly the WP-2 recomposition's problem too; worth pairing them. (N-3) /process/ is a redirect that renders /about/'s markup, so dom-integrity DOUBLE-COUNTS about's changes under two routes — same for /gallery/ against /saunas/. Harmless today (the per-page budget absorbs it and the entries are correct on both) but it inflates the consumption tallies and would mislead anyone reading them as a page census. Record only; do not chase. (N-2, DONE in the fix round) A rhythm comment cited `styles.css:640` for a sentence living at 639 — off by one when written and wronger with every edit above it. Fixed by citing the SELECTOR, not a line number; the predecessor pin's `styles.css:2174` was the same bug and is gone with it. Stylesheet line numbers are not stable addresses and should not appear in any comment. (N-4, DONE in the fix round) The `.section--bleed` gutter pin excused ANY bleed section from the gutter assertion. Zero gutter is right for a photograph and a real defect for a section with copy in it, whose words would touch the viewport edge — not hypothetical, that is exactly what the /saunas/ mosaic did for one commit before Jen ruled it back to contained. Now scoped to bleed sections with no RENDERED text (innerText, not textContent — the video plate's only text is <video> fallback that no visitor sees, and textContent would refuse to excuse the one element the pin exists for). Fixing this surfaced a second latent bug: GUTTER_DEVIATIONS entries were matched with `forClasses` OR `match`, so a class-scoped pin's `match` was never consulted and the new scoping would have been silently discarded. They are ANDed now."
  - "HARNESS, POST-WAVE — two rhythm.test.mjs mutation anchors are NON-UNIQUE in the built stylesheet (`margin-top:var(--spacing-xl)` matches 6 sites, `padding-inline:var(--gutter)` matches 2). The battery patches by string split/join, so these mutate every match rather than the one the mutation names: the probe still moves and the mutation still reports detected, but it is proving something broader than its own description. Pre-existing, found during the deploy-1 fix round (Razor NOTE), deliberately NOT fixed there — out of that round range, and re-aiming a mutation is a harness change that deserves its own review. Fix with a uniquely-anchored patch (include surrounding declarations) or an occurrence index. Companion item for the same pass: the row-signature matcher missing per-pixel tolerance, already queued."
  - "FUNNEL, DEFERRED — the real /contact/ form carries NO honeypot field, while the configurator modal form does (input[name=_gotcha], offscreen, tabindex -1). So the site primary contact route has strictly less spam protection than its secondary one, and the asymmetry looks accidental rather than decided. Pre-existing, surfaced during the deploy-1 fix round (Razor NOTE); no code changed there because it is a spam-posture decision, not a defect in that round work. Decide deliberately: add the same honeypot to the contact form, or record why the two forms differ. Note when doing it that dom-integrity would see the added field as a markup change on every page carrying the contact form, so it needs a declaration."
  - "RESOLVED 2026-08-02 (batch B4-pre) — DOM-INTEGRITY HAD NO `add-subtree` (found 2026-08-02 by MEASURING Wave B-1 batch B4, not by reading code; B4's markup is implemented and every other gate is green, but its dom-integrity certificate cannot be written and the batch is REPORTED rather than closed). The whitelist vocabulary is asymmetric. A removal of a whole component is ONE `delete-subtree` entry: tag + ancestry path + a measured content hash, capped at MAX_SUBTREE_TOKENS (400), impossible to write lazily because the hash must be measured. An ADDITION of the identical component has no counterpart, so it can only be declared one `added` entry per token. Measured on B4 against baseline ab83ed6: /saunas/ shows 355 undeclared ops at each width — 249 removed element/text tokens (coverable by an estimated 6-8 delete-subtree entries) and 102 ADDED element/text tokens (102 entries), plus 2 kind:code entries for the head.njk JSON-LD pair that consume across all 19 pages. Total for a correct, non-widened enumeration: ~111 entries against the plan's ~40 tripwire. THE TRIPWIRE'S OWN REMEDY DOES NOT APPLY: the plan says a batch past ~40 splits rather than widening any entry, and splitting does not reduce this, because the five-row ledger ALONE is 69 added tokens (5 rows x 11, plus the header row and the section wrapper) and no sub-batch can deliver fewer than one row (11 tokens) without shipping a broken page between batches. So the ceiling is structural, not a matter of batch size. A second, independent finding from the same run: an attribute-LESS element can never be named by a `contains` substring at all, so it is undeclarable in principle — B4 hit this with five bare <span> column labels and fixed it by giving them classes (e009f49), but the general gap stands. THE FIX: an `add-subtree` op symmetric to delete-subtree — tag + path + a subtree hash measured from the CANDIDATE, the same 400-token cap, matched against runs of `added` ops, with a checkAddedDeclarations mirror of checkSubtreeDeclarations so a hash matching nothing in the candidate is refused before it consumes anything, and its own mutation probe so the hash check cannot be disabled unnoticed. That would take B4 from ~111 entries to ~15. It is a harness change and therefore its own separately reviewed commit by the harness's implementer, ahead of B4 — the same rule the reveal-pin repair followed (`_notes.stalePin` in visual-diff.config.json -- cited by key; B6's prune moved it from :282 to :56) and the same rule B3 followed when it carried the cap raise as its own subject. Do NOT close B4 by widening entries; that is precisely the tuning-until-green move both instruments exist to refuse. RESOLUTION: `add-subtree` shipped as batch B4-pre (0be8d0d), its own reviewed subject alongside the batch that needed it — the same pattern B3 used for the cap raise. One matcher and one cap serve both directions; checkAddedDeclarations mirrors the staleness check against the CANDIDATE; fixtures Z21/Z21b/Z22/Z23/Z24/Z25/Z26/Z27 mirror the Z-series discipline including consumed===0 asserted explicitly past the cap, and three mutations D-m20/21/22 kill the add path's cap, hash check and staleness mirror separately from the delete path's — D-m20 exists specifically because the shared matcher makes D-m16 LOOK like it covers both, and would stop doing so the moment the directions are split. B4's census went from ~111 entries to 14 (4 delete-subtree, 4 add-subtree, 2+2 kind:code, one removed/added pair for a moved plate) and the certificate is PASS: 38/38 fingerprints identical, zero undeclared, zero stale, zero run failures."
  - "VISUAL-DIFF CANNOT CERTIFY A RECOMPOSITION, and says so honestly (measured 2026-08-02, Wave B-1 B4 against ab83ed6). Blast radius is exactly right: 34 of 38 page/width comparisons are 0.000%, including `/` and including every page that carries the changed sitewide OfferCatalog JSON-LD — which is the proof that the head.njk change is genuinely non-visual. The only movement is /saunas/ and its /gallery/ redirect twin: 55.229% of pixels at 1440 with height -1684px, 64.317% at 390 with height -4761px. FIGURES SUPERSEDED-BY-CONFIG 2026-08-02 (Razor N6): these were measured mid-batch, before the compare-table tier deferral and the fix round. The numbers of record live in scripts/visual-diff.config.json's expectedToChange reasons for /saunas/ and /gallery/, which are re-measured whenever the batch shape moves; read them, not this line. Kept rather than edited because the CONCLUSION it supports -- that fitConfidence, coverage and the width spread are structural and unwaivable -- is unchanged by the re-measure. changedPct and heightDelta are waivable and are already absorbed by WP-1a-era entries whose REASONS now describe the wrong change (B6 was already scheduled to prune them; B4's own entries must replace them). But three results are structural and NOT waivable by design: fitConfidence 0.534/0.580 (below the 0.75 floor), shift coverage 0.149/0.313 (below 0.95), and a width-spread RUN FAILURE of 3014px over a 400px budget. All three are correct. A recomposition that deletes 21 bullet rows, three stat cards and two offering cards removes far more vertical space at 390 than at 1440, so no single scale-and-offset can describe the page and the harness refuses to pretend one does. This is the same class of verdict the DOM-integrity module documents for a typeface swap — 'that verdict is the product, not a defect'. It means the pixel harness is the WRONG instrument for this batch and the DOM certificate is the right one, which is exactly why the add-subtree blocker above is load-bearing rather than bureaucratic. Recorded 2026-08-02."
  - "B4 FIX-ROUND DEFERRALS (recorded 2026-08-02, Razor NOTEs held over rather than done). (N2) TYPOGRAPHIC SPLIT between the two model tables on /saunas/: the ledger applies Jen's two sanctioned glyph substitutions and prints '5' x 7'' with a multiplication sign and an en-dash capacity, while #compareTable, rendered by js/compare.js, prints the raw canonical strings. Both are now GATED against canonical in their own convention (models-json-roundtrip half four), so neither can drift -- what is unresolved is that they disagree with each other typographically. NOT harmonised here on purpose: teaching compare.js the substitutions is a js/ change, and any js/ change flips dom-integrity's stated boundary ('client JavaScript differs ... do not read a PASS here as covering it') for what is currently a typographic nicety. B6, where the harness hygiene pass can absorb the boundary note. (N1) The compare table's priceRange is still hand-written and ungated; the per-model price, size, capacity and heater cells ARE now gated, and the ledger-vs-table price agreement is asserted, so the exposure is one summary string rather than the table. Queue with N2. (N7) IA, no code: home's 'Explore custom builds -> ' and 'View mobile options -> ' both point at /saunas/, where B4 removed the Commercial & Custom section they used to land on. They now dead-end on a page with no custom or mobile content beyond the SC row. This is a copy and information-architecture decision, not a bug to patch: the honest destinations are /commercial/ (WP-4, does not exist) or /contact/. FOR LEE / WP-2. (W2 residual) The ledger's column-label spans carry aria-hidden='false' per Jen's verbatim §1.2, but Chromium marks bare <span>s 'uninteresting' and no StaticText for Model or Price reaches the accessibility tree -- measured via CDP getFullAXTree at 1440. Nothing is lost in practice, because the fix to W2b made each row self-describing ('S4 7' x 7' 4-5 people From $29,000 CONFIGURE'), so the header is visual scaffolding for sighted readers and the rows carry the facts for everyone else. Making the labels genuinely exposed means real table semantics, which conflicts with the row-as-single-button contract three suites depend on. Flagged to Jen and Razor rather than solved by inventing ARIA roles Jen did not specify."
  - "KNOWN-BAD COMMIT RANGE, Wave B-1 branch relay/redesign-wave-b1: fd7194d, 0d30243 and 70ec7cb DO NOT BUILD. `npx eleventy` fails on all three with 'filter not found: jsonld'. Cause, and it is mine (2026-08-02, B4 fix round): the `| jsonld` holes were edited into src/_includes/pages/saunas.njk and src/_includes/head.njk while the filter that implements them was added to .eleventy.js, and then the templates were swept into the W2 commit by a path-scoped `git add` three commits BEFORE .eleventy.js was committed with the N8 work. Every commit from e791132 onward builds, and so does everything before fd7194d. FOUND BY THE HARNESS, not by review: visual-diff fixture O4 runs the real CLI against HEAD~1 and HEAD, so a non-building HEAD~1 turned the suite red and named the missing filter. That fixture exists to prove the redirect gate is wired in; it caught an unrelated bisect defect as a side effect, which is the argument for end-to-end fixtures in one line. NOT REPAIRED BY REWRITING HISTORY: the `git reset` needed to re-order those commits was refused by the workspace safety guard, and working around a guard to tidy a git log is a bad trade. The range is recorded here instead. CONSEQUENCE FOR ANYONE BISECTING this branch: those three commits will fail to build for a reason unrelated to whatever is being bisected; skip them (`git bisect skip`). The lesson is the generalisable part: a filter and its first use are ONE change and must land in ONE commit, and a path-scoped `git add` will happily commit half of a change that a whole-tree `git add -A` would have kept together."
  - "TOKEN QUEUE — `--nav-clear`. `.page-hero { padding: 12rem 5% 6rem }` (cited by SELECTOR, not line: this entry said styles.css:730, which was ALREADY wrong when B6 checked it — the rule sat at :834 at B5's tip and moved to :845 when B6 added the W5 comment above it. Third instance of B5's N-2 defect found in two days; stop writing stylesheet line numbers into prose) carries a 12rem top that reads as NAV CLEARANCE rather than as a rhythm value, which is why it must not be migrated into the `--section-pad` tier system along with everything else. Naming it (`--nav-clear`) would make the intent explicit and let the tier work proceed without it. NAV HEIGHT NOW MEASURED (2026-08-02, deploy-1 fix round), which removes the blocker this entry recorded: the fixed nav is 148px at desktop and 91px at <=768px, and both figures are DERIVED rather than pasted — logo + the nav's own block padding + its 1px rule (115+32+1 and 58+32+1). That derivation now ships as `--nav-band` and is machine-checked by rhythm.test.mjs group N: N1 resolves the token at a phone AND a desktop width and asserts it equals the nav's measured height, so a literal pasted over it fails. `--nav-clear` (the .page-hero 12rem question) is a SEPARATE token and is still queued — but it can now be settled against a real number instead of an inferred one. Deliberately out of scope for Wave B-1 (doc 21 §6.2a leaves nav 2.5% and .page-hero 5% unmigrated); queued. Recorded 2026-08-01."
  - "FLAKE WATCH — quote-funnel:test, TWO transient failures, no root cause yet (opened 2026-08-02, Wave B-1 B6; Razor amendment folded in). OCCURRENCES: (1) batch B3, one round failed 88 of 89 assertions then passed clean on re-run; (2) batch B5 ruling-3 round, one EXIT-1 followed immediately by 5 consecutive clean runs. Both were transient and neither reproduced. WHAT IS REQUIRED BEFORE THIS CAN BE DIAGNOSED: a FULL LOG capture on the next occurrence. Both existing captures are tail-2 and therefore unactionable — a tail shows the exit and hides which assertion went first, and for a suite of 89 assertions the identity of the FIRST failure is the entire diagnostic signal. Do not close this item on a third tail-2. ROOT-CAUSE HYPOTHESIS, not yet tested (Razor N-4 line of reasoning): the suite REBUILDS dist/ in place partway through its battery, so any concurrently-running suite that reads dist/ — and most of them do — can observe a half-written tree, and quote-funnel can equally be reading a tree another suite's build is rewriting under it. That explains transience, the all-at-once 88/89 shape (a page served mid-write fails nearly every assertion about it), and why re-runs are always clean. INTERIM DISCIPLINE, adopt now rather than waiting for the fix: SERIALIZE. Do not run quote-funnel concurrently with any other dist-reading suite; the twelve-suite gate battery runs sequentially for this reason and any parallelisation of it must exclude quote-funnel until this is understood. THE REAL FIX when it comes is almost certainly to give each suite its own build directory rather than sharing one mutable dist/, which is a harness change and gets its own review."
  - "POST-WAVE HARNESS PASS — CONSOLIDATED INDEX (opened 2026-08-02, Wave B-1 B6; ONE entry so the queue has one address, with pointers rather than copies so nothing is duplicated or drifts). Nine items, none deploy-blocking, all deliberately deferred out of Wave B-1 because each is an instrument change and this wave's standing rule is that an instrument change is its own separately reviewed subject. (1) ROW-MATCHER PER-PIXEL TOLERANCE, visual-diff — the row-signature matcher has none, which is what made B1's hero-photograph resampling read as a 51px@1440 shift with coverage 0.926@390 when zero rows below the hero band moved; evidence and full argument in the REFUSED RESIDUALS entry above. Highest value of the nine: it is the one that would retire a whole class of false red. (2) NON-UNIQUE RHYTHM MUTATION ANCHORS — `margin-top:var(--spacing-xl)` matches 6 sites and `padding-inline:var(--gutter)` matches 2, so those mutations patch every match rather than the one they name; full record in its own entry above, not repeated here. (3) REDIRECT-STUB ROUTE DOUBLE-COUNT — /process/ renders /about/'s markup and /gallery/ renders /saunas/'s, so dom-integrity books those changes under two route names each and the consumption tallies read high; harmless to the gates, misleading to anyone reading them as a page census (Razor B5 N-3). (4) W-2 TIER-FIDELITY GATE, the real gap of the nine — no instrument knows WHICH section was supposed to get which tier, so swapping two assignments between pages leaves every gate green; sketch is a checked-in per-route manifest (route -> ordered list of section identifier + expected tier) asserted against the rendered order, and its cost is a manifest a human has to keep true, which is WP-2's problem too and argues for pairing them. (5) Z28 COMMA-DANGLE SENSITIVITY — the Z28 wiring fixture (`grep -n Z28 scripts/dom-integrity.test.mjs` -- the fixture NAME is the address) matches on source text and is sensitive to a trailing comma, so a formatting-only edit can break a fixture that is testing wiring rather than formatting. (6) M12 DISCLOSURE COMPLETION (`grep -n M12 scripts/events.test.mjs` -- unique) — the mutation displaces the choreography clock origin, and the honest disclosure is that the HISTORICAL clock-bug instance is caught only about 58% of the time by tolerance alone; what actually covers that regression is the structure, not the probability — the agreement invariant plus the single-clock design mean there is only one clock to displace. Write that into the fixture's own `proves` text so nobody reads 58% as the coverage. (7) /contact/ HONEYPOT ABSENCE — the primary contact route has strictly less spam protection than the modal; its own entry above carries the decision framing and the dom-integrity consequence. (8) priceRange, hand-written and UNGATED — `\"priceRange\": \"$22,500 - $57,000+\"` in src/_includes/head.njk (`grep -n priceRange src/_includes/head.njk` -- unique in that file; the KEY is the address, deliberately not a line) is a summary string no suite checks, while every per-model price around it is now gated against canonical; pairs naturally with item 9. (9) LEDGER/COMPARE TYPOGRAPHIC SPLIT — the /saunas/ ledger applies Jen's two sanctioned glyph substitutions and js/compare.js prints raw canonical strings, so the two tables disagree typographically while both are correctly gated; the reasoned deferral lives in the models-json-roundtrip comment and STANDS — harmonising it means teaching compare.js the substitutions, which is a js/ change, and a js/ change flips dom-integrity's stated markup-only boundary for what is a typographic nicety. B6 was named as the place to absorb that boundary note and this is it: the boundary is unchanged, the deferral is re-affirmed, and it is recorded here rather than left in a script comment nobody greps."
  - "WAIVER PRUNE — GATE CONSEQUENCE, MEASURED AND DELIBERATELY NOT PAPERED OVER (2026-08-02, Wave B-1 B6). After pruning 15 expectedToChange entries and all 15 pageOverrides, visual-diff was re-run against ab83ed6: 32 of 38 page/width pairs FAIL and 6 PASS, plus 2 route-level RUN FAILURES (/saunas/ and /gallery/, a 3012px width spread each) — which is the 34 the harness prints as its failCount. Stated as 32+2=34 rather than as a bare 34 because the two numbers count different things, and a reader comparing 'pairs' against 'items' will otherwise think a pair went missing. The six passing pairs are /booking-ops.html, /contact/ and /locations/ at both widths. THE QUESTION THE PRUNE HAD TO ANSWER was whether any removed waiver was load-bearing — whether something had been silently leaning on it. ANSWER: no waiver was propping up a legitimate pass, but the prune DOES make previously-hidden movement visible, and that movement is real. The pruned expectedToChange entries waived changedPct and heightDelta on 15 Wave-A routes; those same two metrics are now red on most of them. They are red HONESTLY: B5 wired 26 sections to tier classes and Ruling 3 changed 11 elevated grounds across six-plus routes (service-area.njk:45 alone is five of them), so /about/, /warranty/, /faq/, /404.html and the five service-area pages genuinely moved between ab83ed6 and this tip. A Wave-A-era waiver left in place would therefore have SILENTLY COVERED B5's changes under a reason describing WP-1a's type system — the precise hazard the prune exists to remove, demonstrated rather than argued. WHAT IS NOT DONE, on purpose: those routes are NOT re-declared with B5-accurate reasons. Writing thirteen fresh waiver entries in a docs batch to bring the board back to green is the tuning-until-green move both instruments exist to refuse, and B6 is the wrong batch to review thirteen new declarations in. They stand as honest reds and retire at the Deploy-2 baseline advance with the rest of the refused residuals. If a future batch wants them declared instead, that is its own reviewed subject with its own measurements. METHOD CAVEAT, stated so nobody over-reads the number: the paired counterfactual run — the same comparison with the PRE-prune config, which would have given an exact before/after FAIL delta — was attempted and CRASHED on a `page.waitForFunction` capture timeout thrown from `captureAll` in scripts/lib/capture.mjs (`grep -n captureAll scripts/lib/capture.mjs` -- one definition; the file has two waitForFunction sites, so the FUNCTION is the address), a transient of the same family as the quote-funnel flake logged above. The config loaded cleanly; nothing about the prune caused it. So the before/after attribution above is derived from the pruned entries' own contents (which metrics they waived, on which routes) rather than from a paired run, and it is stated as the analysis it is. Re-running that counterfactual is the cheap way to convert this from reasoned to measured, and it is worth doing if anyone doubts the conclusion."
  - "JEN STAGE 3 — WAVE B-1 DESIGN GATE: PASS, with one in-wave fix (recorded 2026-08-02). THE FIX, ridden in B6 and the only code Jen's review put in this wave: W5, `text-transform: uppercase` added to `.eyebrow--wide`. Her ruling was FIX, not amend — the class is written as a modifier of `.eyebrow` but its single consumer (its one consumer, the 'Standard Builds' kicker -- `grep -rn \"eyebrow--wide\" src/` returns exactly one line) wears it ALONE, so it never inherited the base class's uppercase and the site's one section kicker rendered sentence-case under a 0.2em tracking cut for capitals. Declared on the modifier rather than patched in the markup because the kicker is all-caps BY DEFINITION. Blast radius verified before riding it: grep returns exactly one consumer, on /saunas/, a route already red and declared in visual-diff, so no new route moves. DOCKET RULINGS, recorded where B6's docs touch them: (b) the Ruling-3 SIDE-EFFECT PAGES are CONFIRMED CLOSED — the six routes the W-3 conversion spread the double-gutter to are correct at both widths and Jen accepts the elevated column landing on the same x as a plain section.container as the statement of the ruling. (c) the FOUR §3.14 ADJACENCY UNDER-COUNTS are WP-2 items, not defects in this wave; Jen will amend her spec's count at her next touch of it, so the spec and this ROADMAP will disagree on that number until she does — read the spec as low by four in the meantime. (d) the BARE-IMG PLATE INTERPRETATION is ADOPTED INTO SPEC DOCTRINE: a full-bleed figure sits BETWEEN sections and carries no rhythm of its own, which is the `.section--bleed` rule in styles.css. Jen cited that rule as `styles.css:639`; it is recorded here by SELECTOR instead, because B6's own W5 edit moved it — a live demonstration of B5's N-2 ruling that stylesheet line numbers are not stable addresses, arriving within hours of the ruling. C-2 (compare-table typography) and C-3 (centered thresholds) are recorded as WP-2 notes — both are composition questions that belong with the recomposition work, neither is a Wave B-1 defect. Jen's observation of a TRACKER CONSOLE ERROR is the already-known draft-origin validation failure; see the receipt-protocol record in the Wave B-1 shipped entry rather than treating it as a new finding."
  - "HARNESS ARITHMETIC — R2 CONFORMER RECONCILIATION, RE-MEASURED AND A CORRECTION REFUSED (2026-08-02, Wave B-1 B6). B6 was tasked to record a corrected reconciliation for commit fcf8a87's claim that R2 conformers went '76 -> 62, which is exactly the 14 elevated grounds moving from conforming to pinned', on the basis that the commit's arithmetic was wrong and the true figures were 72-14+2+2=62 with pinned 22+14-2=34. THAT CORRECTION DOES NOT REPRODUCE AND WAS NOT WRITTEN. Measured directly, by building each commit in a throwaway worktree and running `npm run rhythm:test`: 9747e25 (pre-B5) = 74 conforming · 117030b = 76 · 706f43c = 76 · fcf8a87 = 62. R2e reports 14 elevated grounds found and the percentage-gutter pin count is 2, both at the current tip. So the commit's own span is its parent 706f43c (76) to itself (62), a delta of exactly -14, and its stated attribution to the 14 elevated grounds is arithmetically and causally correct as written. The figure 72 appears at NO commit in the B5 range. The nearest true statement in the shape the correction was reaching for is the WHOLE-B5 chain, 74 + 2 - 14 = 62: +2 landed at 117030b (the C-1/W-3 mosaic and full-width-ground round) and -14 at fcf8a87 (Ruling 3). That is one +2, not two, and it starts at 74, not 72. NOT RECORDED, deliberately: the pinned-side figure '22+14-2=34'. rhythm.test.mjs counts `pinned` but only prints it in a FAILING check's hint, so a passing run does not expose it and it cannot be confirmed without instrumenting the suite — which is a harness change and out of a docs batch's scope. An unverified number is exactly what this item exists to stop propagating, so it is left unwritten rather than copied forward. ESCALATED for adjudication: either the re-review's 72/22 came from a measurement this method does not reproduce, or it was itself an arithmetic slip. Anyone re-opening it should re-run the four builds above before trusting either version. THE LESSON, which is why this is a ROADMAP entry and not a commit-message footnote: a batch chartered to fix three wrong numbers nearly wrote a fourth, and the only thing that stopped it was measuring a figure that had arrived pre-labelled as the correct one."
testing: "TWELVE test scripts plus lint:css (stylelint itself) — counted from package.json 2026-08-01, correcting the earlier 'ten suites' claim which under-counted and elided two real suites: visual-diff:test (repaired, affine model, fail-closed) · dom-integrity:test · quote-funnel:test · events:test · fonts:test · rhythm:test · package-claim:test · prices-version:test · build-cache:test · models-json:roundtrip · lint:css:test (scripts/stylelint-gate.test.mjs — a real suite, not part of lint:css) · csp-hash:test (npm name is csp-hash:test, NOT csp-inline-hash; the script file is scripts/csp-inline-hash.test.mjs and the two names have been confused before). Run any as npm run <name> (see package.json // comments for the ones needing a build or a sibling ../ssc-ops checkout first). models-json:roundtrip: RUN 2026-08-01 on this branch — ROUND-TRIP CLEAN, exit 0, 156 PASS / 0 FAIL, including the canonical byte-identity half (~/marvin/content/reference/operations/models.json === docs/redesign-2026-07/41-models-v2.json, 9644 bytes). The 'correctly red until the MARVIN merge lands' state is OVER: the canonical file landed 2026-07-31 and the suite is green by observation, not by inference."
pinned: false
shipped:
  - date: 2026-08-02
    item: "WAVE B-1 — THE WAVE, CLOSED ON THE BRANCH AND NOT YET ON PRODUCTION. Read the last clause first: this entry records work that is COMPLETE AND REVIEWED on relay/redesign-wave-b1, and NOTHING in it is live. Two deploys are pending and are the only thing between this and production. THE NINE UNITS OF WORK: B0 docs reconciliation · B1 hero-first reveal + LCP delivery · B2 spacing, CSS-only (the F-1 container-padding root cause) · the DEPLOY-1 FIX ROUND (the hero/nav collision at 390, the scrim, the wrap census) · B3 modal scoping + the MAX_SUBTREE_TOKENS cap raise · B4-pre the add-subtree primitive · B4 /saunas/ recomposed as a Ledger + the model-fact unification · B5 section-tier assignment · B6 wave-closing docs + harness hygiene. The count is NINE UNITS OF WORK and the D1 fix round is the ninth: an earlier draft listed the Deploy-1 GATE in that slot, which double-counted a checkpoint as a batch and silently dropped the fix round that actually shipped code. A gate is a decision point, not a unit. B4-pre is counted separately on purpose: it is an INSTRUMENT batch, carried as its own reviewed subject ahead of the batch that needed it, which is this wave's standing rule and was followed three times (B3's cap raise, B4-pre's add-subtree, and the reveal-pin repair before that). REVIEW: Razor passed every unit, B6 included — B0 PASS · B1 PASS · B2 PASS · B3+D1-fix PASS (round 2; the mechanism refused both constructed breaks) · B4-pre+B4 PASS (the primitive refused all constructed breaks including a one-character price drift) · B5 PASS after a three-ruling fix arc · B6 PASS, CODE-COMPLETE, with a docs-only touch-up round that Razor explicitly scoped as 'not a fix round'. B6's verdict EXISTS and is recorded here rather than left as the pending item an earlier draft implied. Coverage was tracked per batch (B0 9/10 + 1 PARTIAL fixed; B1 10/10; B2 10/10). Jen ran Stage 0.7 (the visual spec governing B4's row register and B5's tier assignments) and Stage 3, which PASSED with exactly one in-wave fix: W5, uppercase on `.eyebrow--wide`. HARNESS CAPABILITIES ADDED — the wave built instruments as much as pages, and this is the part most likely to be forgotten: `add-subtree`, the mirror `delete-subtree` never had, with checkAddedDeclarations, one shared matcher and cap for both directions, the Z21-Z28 fixture mirror and three add-path mutations (D-m20/21/22) that exist because the shared matcher makes D-m16 only LOOK like it covers both; MAX_SUBTREE_TOKENS 64 -> 400, argued from measurement and fixture-proven at both bounds; models-json-roundtrip's third half, which reads each RENDERED row's capacity/size/price back out of a real browser and compares to canonical modulo two sanctioned glyph substitutions; rhythm's R2b/R2c/R2d standard/tight/open tier assertions, R2e proving the canonical G RELOCATED the gutter rather than losing it, and group N pinning `--nav-band` to the nav's measured height so a literal pasted over it fails; quote-funnel's presence invariant asserted in BOTH directions, the durable half being that a future page setting `configurator: true` without triggers now fails; the fingerprintsIdentical vacuity guard (Y13/Y13b + D-m19); and two latent harness bugs found and fixed in passing — GUTTER_DEVIATIONS matching `forClasses` OR `match` instead of ANDing them, and the `.section--bleed` pin excusing bleed sections that contain real text. THE MODEL-FACT UNIFICATION: src/_data/models.json is now the single place the site states a model fact, rendering the ledger rows, the ItemList and the sitewide OfferCatalog. It is gated, which is the only thing that makes it a fix. It caught a real live defect — the SC Commercial capacity had drifted to FIVE distinct wrong spellings across five source sites while every existing suite pointed at the configurator, where the number was correct all along. Lee's veto on the canonical 10-12+ figure is still open and is a one-line override if the physical truth is otherwise. WHAT IS RED AND CORRECTLY SO: visual-diff cannot certify a recomposition and says so. Measured against ab83ed6 with the B6 tree: /saunas/ and its /gallery/ redirect twin fail fitConfidence and shift coverage and produce a 3012px width-spread RUN FAILURE, all three unwaivable by design, because a recomposition removes far more height at 390 than at 1440 and no single scale-and-offset describes that. The DOM certificate is the right instrument for that change class and it PASSES. HOME'S RESIDUAL, stated at the baseline that matters: `/`@390 carries an 89px shift with heightDelta -89 and global offset -58 measured BASELINE-RELATIVE against ab83ed6. Quote 89, never the 42px figure that appears in fcf8a87's commit message — 42 was measured intra-round against that commit's own parent and is a true number about a different comparison; against the deploy baseline the figure is 89. `/` carries no shift override and never will, by rule: the F-series calibration fixtures evaluate route '/' against the shipped config, so an override there disarms the harness's own self-test. HERO READING NOTE: the hero_hold cutover date is still a PLACEHOLDER and stands as one until Deploy 1 merges — the merge commit fills it in. Data branched at the old 1240ms boundary must not be pooled with data after it, so the placeholder is load-bearing, not a formatting gap. RECEIPT PROTOCOL — OUTCOME RECORDED, GATE NOT YET CLOSED: the draft-URL probe was RUN. Result: accepted with an HTTP 200 and a success body, never delivered. That is the SAME accepted-200-never-delivered shape as the 2026-07-30 localhost result, now reproduced from a genuine *.netlify.app draft origin. It is AMBIGUOUS and must not be read as 'the funnel is broken': the protocol's own interpretation guard says a draft-origin test can PROVE success and never failure, because a Formspree domain allowlist would produce exactly this result for a perfectly healthy form. Independent evidence that the production path is healthy: a GENUINE customer inquiry arrived 2026-08-02 through the live site. So the draft probe settled the draft-origin question (it discards) without settling the production question (which looks fine). CONSEQUENCE: the hard gate is not satisfiable from a draft URL, the STAGED FALLBACK is armed for both merges, and its watcher clause is the part to honour — the merging session owns the T+60 inbox check and must still be live to run it; if it cannot be run for any reason, roll back rather than wait. Lee's dashboard check should confirm the allowed-domains setting, which is what would turn this from ambiguous into explained."
  - date: 2026-08-02
    item: "WAVE B-1 BATCHES B4-pre + B4 — /saunas/ recomposed as a Ledger, the four capacity copies collapsed to one, and the DOM harness taught to certify an ADDITION. THE PAGE: doc 10 §3.2 and Jen's Stage 0.7 spec reorder /saunas/ around the models. Nine sections, §2.2-clean: hero -> model index (E, tight) -> plate -> compare table -> plate -> comparison essay (D) -> science thesis (B, open) -> Our Work -> threshold (G, elevated). Five rule-separated rows replace the card grid, each ONE actionable element carrying data-action + data-model + role=button + tabindex=0 + aria-label (three suites click that compound selector; init.js synthesises Enter/Space off the role); hover is the COLOUR half of the §5.2 grammar only, because a row that lifts tears the hairline it shares with its neighbours (Jen's own §1.6 amendment). Removed: the three comparison cards and all 21 tick/cross bullets (two paragraphs and a /faq/ link instead), the 66% and 24% stats and the elevated overlay treatment (one 40% stat, 34rem, on ground, claim unaltered), and 'Commercial & Custom' outright. Case-study units ship ZERO — no gallery image on this site is attributed to a build, and assembling units from unattributed photos is fabrication in its most deniable form. THE FACTS: src/_data/models.json is now the one place the site states a model fact, rendering the rows, the ItemList and the sitewide OfferCatalog. That is only a fix if it is gated, so models-json-roundtrip gained a third half — name/size/capacity/basePrice byte-equal to canonical per model, priceDisplay carrying exactly basePrice, the roster compared, and each RENDERED row's capacity/size/price read back out of a real browser and compared to CANONICAL modulo the two sanctioned glyph substitutions. It would have caught the real defect: the SC capacity was '10-14+' in the ItemList, '10-14' in the card, '10-14' in the card's alt and '10-14' in the OfferCatalog, against a canonical '10-12+' that js/data.js had right all along — the markup was the drifted copy, in four places, while every suite pointed at the configurator where the number was correct. Mutation-verified in both directions, including the historical shape (markup hard-codes a capacity while the data file stays canonical -> RED). THE HARNESS (B4-pre): `add-subtree`, the mirror delete-subtree never had. Found by measuring, not reading: 249 removed tokens took ~7 entries while 102 added tokens took 102, and splitting could not help because the ledger alone is 69 added tokens. Attribute-less added elements were undeclarable in principle. One matcher and one cap for both directions, staleness checked against the candidate, full Z-series fixture mirror, three add-path mutations. CERTIFICATE: dom-integrity PASS, 38/38 fingerprints identical modulo 14 measured declarations, zero undeclared, zero stale, zero run failures, jsChanged false. Twelve suites green. Also: rhythm's R-m4 re-aimed at home after the battery itself reported the probe had lost its coverage on /saunas/; the fingerprintsIdentical vacuity guard Razor ordered (Y13/Y13b + D-m19). CARRIED, not hidden — visual-diff stays RED on /saunas/+/gallery/ for fitConfidence, shift coverage and a 3062px width spread. All three are correct and unwaivable by design: a recomposition removes far more height at 390 than at 1440, so no single scale-and-offset describes the page. The pixel harness is the wrong instrument for this change class and says so; the DOM certificate is the right one. Deferred to B5 by measurement rather than preference: the compare table's tier class, because re-tiering a SURVIVING section is a removed/added pair on one <section> whose `class=\"container\"` matches four siblings, and no entry for it can be specific — B5 re-tiers the whole page at once and it becomes declarable."
  - date: 2026-07-31
    item: "REDESIGN WAVE A — the full relay (branch relay/redesign-wave-a, 8 work batches + 8 instrument/parity commits, 15 Razor passes, 1 circuit-breaker trip resolved by Lee, behavioral eval + Jen gates). Shipped: pre-flight (content-hash cache keys fail-closed, build cleaning, pinned reproducible builds); the visual-diff harness repaired to genuinely fail-closed (budgets, per-metric waivers, pageOverrides raise-never-disable, affine global-offset model with spread residuals, ~15 mutation classes killed) + a DOM-integrity certificate (delete-subtree/renameMap/kind:code vocabulary, range-scoped whitelist, content-hash identification); parallax flake root-caused and killed; quote integrity (bench choice reaches quotes, WiFi $2,000 line — corrected a LIVE false price claim: the package cost $1,000 MORE than its parts while advertising Save $1,000; now guarded by the package-claim detector); THE FUNNEL rebuilt (two-step in-modal form, 13 browser-verified states, keyboard-complete incl. previously-unreachable model cards, localStorage with pricesVersion stamps + step-1 persistence + race-proofed clears, PIPA disclosures in-flow, quote_submit_success wired); measurement (12 events, structural PII denylist, double-count suppression proven against the real shared tracker); the TYPE SYSTEM (87 declarations → 10 tokens, self-hosted subset variable fonts 99KB, zero Google origins, tnum preserved, 19px/11px floors, quote-btn contrast 1.28:1 → 9.02:1); COLOUR/RHYTHM/MOTION (169-site vocabulary migration, ember/ink/ground system, P0-3 killed, intro+parallax deleted with hero events re-based, stylelint permanence gate, computed-rhythm suite); THE REPRICING (all 19 doc-35 rows, Save $500 true by construction on all five models — verified by three independent derivations, v1-restore recompute proven, prices-version lock, models.json v2 parity check correctly red until the MARVIN merge (RESOLVED 2026-08-01: canonical landed, suite RUN and green)). Ten suites, 454+ assertions, zero waivers anywhere. [Count corrected 2026-08-01: 'ten suites' under-counted at write time — package.json defines TWELVE test scripts plus lint:css; see the testing field. The shipped work itself is unchanged, only its description.] Evidence corpus: docs/redesign-2026-07/ incl. 40-wp1a-anomalies.md."
  - date: 2026-07-28
    item: "Security + measurement. Deleted an orphaned legacy analytics stack (own tracker, reader, dashboard) whose unauthenticated reader served page URLs and titles for every property posting to the shared store, including fern-app.netlify.app. Removed passthrough copies publishing function source and the database schema. Root-caused a resurrection: Eleventy does not clean its output dir and Netlify caches it, so deleted files returned on the next auto-build — build command now cleans first. CSP fixed to allow the tracker origin; the site had been recording no analytics at all. Verified 404 on apex and deploy permalink."
  - date: 2026-07-28
    item: "Visual-regression harness (scripts/visual-diff.mjs). Builds two git refs, captures 19 pages at 1440/390, gates on a structural layout-shift metric plus changed-pixel percentage. Repair pending per critic rev.3."
  - date: 2026-07-28
    item: "Quote-integrity corrections: build duration was 4-6 weeks in the FAQ against 8-12 in commercial quotes — now 'as little as 8-12 weeks' in both answer and schemaAnswer."
  - date: 2025-12-15
    item: Static site live on production (eleventy + Netlify)
---

# SSC Website — Roadmap

## Redesign 2026-07 — where everything lives

A full redesign was specified in July 2026: three specialist audits, five Creative
Department specifications, and three critic passes. **Nothing in `docs/redesign-2026-07/`
is a draft — each document is the authority for its lane**, and the plan sequences
them rather than restating them.

| Doc | Authority over |
|---|---|
| `00-design-brief.md` | Context, Lee's fixed decisions, verbatim voice source |
| `01-audit-functional.md` | Forms, the configurator failure, security, data sync |
| `02-audit-copy.md` | Copy audit (superseded by 13) |
| `03-audit-ux.md` | UX/UI findings with line numbers |
| `04-brand-guidelines-updated.md` | Replacement brand guidelines |
| `10-jen-art-direction.md` | Composition, archetypes, page structure, motion, case-study template, `/commercial/` + `/care/`, mark phase |
| `11-beatrice-typography.md` | Type system, fonts, scale, web-font performance |
| `12-saul-visual-photography.md` | Photography direction + shot lists, grid, mark, Cloudinary delivery |
| `13-george-copy.md` | All copy, microcopy, meta — source-annotated |
| `14-wim-journey-funnel.md` | Configurator flow, journeys, booking states, analytics events |
| `15-mood-board.html` | The approved visual direction, as a working page |
| `20-fact-gathering-questions.md` | **The questions whose answers become published copy, and the answers received so far** |
| `21-resolved-tokens.md` | **Every contested shared token. Overrides 10–14 on those values.** |
| `30-pricing-heaters-equipment.md` | Heater + equipment market pricing, sourced |
| `31-pricing-materials.md` | Material + fabrication pricing, sourced |
| `33-intake-form-design.md` | Contact form field set, evidence, Formspree capabilities |
| `34-design-fee-conventions.md` | Design-deposit conventions + draft copy |
| `35-configurator-price-sheet.md` | **Every configurator price, resolved.** One convention, 19 lines, arithmetic shown |
| `HANDOFF.md` | **Start here on a cold pickup** |

**Plans and reviews** in `.claude/plans/`:
- `redesign-wave-a.md` — the relay-ready plan for everything not blocked on Lee
- `website-redesign-2026-07.md` — the full six-package plan (rev. 3)
- `website-redesign-2026-07-critic{,-rev2,-rev3}.md` — three critic passes

**Docked pending Lee's answers** (see doc 20): the process page facts, warranty
terms and the certifying body, the design-deposit decision, per-model price
display, and five smaller copy calls. Case studies are parked separately while
Lee agrees curated packages directly with Clarke, Emmanuel and Mountain Life.

---

## Ideas parked for later

Order is not a priority ranking.

## B7 rides (recorded 2026-08-02)

**Hero graceful-cancel — SHIPPED 2026-08-02**, on the fifth mechanism and the first one
that did not fight the browser. Lee's decision was never in doubt: an early scroll/key/tap
during the held beat gently fades the nav and title in rather than snapping them.

COUNTING, because the earlier wording made these look like contradictions and they are
not. FOUR mechanisms were tried and failed (inline longhand overrides; rAF-separated
retiming; pin-and-release; cancel-then-retime). The "two-strike rule" that stopped the
work is the name of the policy, not a count of attempts: it fired after the second
strike, by which point four mechanisms had been tried across them. That is the whole
reconciliation of the 4-versus-2 discrepancy this entry used to carry.

Do not go looking for the attempts in the log (Razor N7). An earlier version of this
paragraph cited them by sha, including one that was amended away rather than reverted
and so never existed in any reachable history. A citation the reader cannot check is
worse than no citation, because it sends them hunting for a commit that is not there.
What IS verifiable is the list of four mechanisms above, the fact that none survives in
the working tree, and `js/animations.js`, which names each one at the point it would
otherwise be reintroduced — which is where the next person actually needs the warning.

ROOT CAUSE — corrected 2026-08-02 (Razor W-3), and the correction matters because the
old story sent readers to the wrong place. This paragraph used to say the pending
transition was "created because `animations.js` adds the `reveal` class to the nav after
transitions are enabled, so the nav's hidden state arrives as a value change under a live
transition." That is wrong. Traced against `93fab50` — the last B7 commit BEFORE the hold
was rewritten, and therefore the last state in which the old mechanism can be reproduced
at all (Razor N6) — by snapshotting
`nav.getAnimations()` on every class mutation:

    t=427  nav class -> "reveal"          anims=[]        <- no live transition
    t=430  .reveal-ready landed           anims=[]        <- still none
    t=484  nav class -> "reveal seen"     anims=[opacity dur=1000 delay=1600 running,
                                                 transform dur=1000 delay=1600 running]

The hidden state never arrived as a value change under a live transition; there was no
transition to arrive under. The real cause is the pair already described correctly by
`RevealManager.scheduleHold`'s docblock in `js/animations.js` and by the `--hero-hold`
token's comment in `styles.css` — named by their docblocks rather than by line numbers,
per B5's N-2 ruling that a line number is not a stable address, and because the numbered
citation this replaces had already drifted (Razor N5): the IntersectionObserver granted
`.seen` almost immediately (the nav is above the fold — 54ms after `.reveal-ready`, not
1600ms), and the CSS carried `transition-delay: 1600ms`, so the transition created at
that moment sat in its DELAY PHASE for the length of the beat. When a cancel then changed
the target value, Chrome UPDATED that delay-phase transition rather than creating
a new one, and the fade inherited the hold's timing no matter what the computed
`transition-delay` said. Measured directly: computed `0s / 0.35s`, actual running animation
`1600 / 1000`. Cancelling the pending animation first removed the hijack and the transition
together, so it snapped.

The fix removes the pending transition instead of arguing with it. **The hold moved out of
CSS and into the reveal clock**: `transition-delay: var(--hero-hold)` is gone from the held
pair, and `RevealManager` withholds `.seen` until `startedAt + HERO_HOLD_MS` instead. No
long transition is ever pending, so the cancel path — clear the timer, add `.reveal--quick`
and `.seen` in one style recalculation — gets an ordinary first transition at 350ms.

This is **not** a second motion system and doc 21 N2 is not superseded. It is the same
`RevealManager`, the same `.reveal` transition, the same `--i` stagger and the same
observer; the `.seen` grant was already JS-driven, and only its TIMING moved, from a CSS
property to the clock the manager already keeps. `--hero-hold` remains the one authoritative
value — `animations.js` reads it off the computed root rather than carrying a copy.

`SETTLE_MS` is unchanged at 2720 and the derivation is unchanged: `.seen` used to land at
t=0 with the transition waiting out a 1600ms delay, and now lands at t=1600 with the
transition starting at once. Same visible timeline, same boundary. Measured on the built
site at both widths: hold path grants at ~1596ms and the title composes at ~2690ms; cancel
path grants at 0ms from the input and composes in 329–382ms; reduced motion composes
instantly. Covered by four new fixtures and three new mutations (M14–M16) in the events
suite, including the behavioural hold check that a computed style can no longer make.

**The pre-ready window (Razor W-1, fixed 2026-08-02).** The first cut still snapped in one
place, and it was the place the feature is most about. Both `.reveal--quick` rules are
gated on `.js.reveal-ready`, but the escape hatch binds one double-rAF EARLIER — 53–168ms
in the field, unbounded in a rAF-starved tab — deliberately, because that gap is when an
impatient returning visitor acts. An input inside it composed elements no transition rule
matched. Measured: zero intermediate frames at 0–40ms, fades only from 80ms on.

A pre-ready release now LATCHES and composes on the first frame in which a fade is
possible (one frame after `.reveal-ready`, not zero — composing in the same task the class
lands in does not transition, because the property becomes applicable in the same style
change as the value). Rejected alternatives are recorded in `js/animations.js`: a
`.js:not(.reveal-ready)` quick rule arms a transition before the double rAF has committed
the hidden state, which is the visible-to-hidden bug that ordering exists to prevent.

That fix then exposed a SECOND instance of this batch's own hijack, worth recording
because it will regrow the same way: `init()` runs twice (DOMContentLoaded and window
load), the second call raced the deferred compose, `.seen` landed alone with a 1000ms
transition and `.reveal--quick` arrived a frame later into a transition already running.
Chrome updated it rather than replacing it — computed duration `0.35s`, actual fade 914ms.
The general rule this batch keeps re-learning: **the held pair must have exactly one
writer.** `pendingQuick` is now never cleared and `quickScheduled` makes the scheduling
idempotent. Guarded by a fixture that FORCES the window (rAF throttled to 150ms — the
starved tab the code names) and asserts it landed there before asserting anything else,
plus mutations M17 and M18.

**Booking subdomain.** `book.secretsaunacompany.ca`, a separate Next.js app, fire-ban
gated. Session photos get the same imaging discipline the site now has (angle in the
source URL, width left to the generator) when it reopens. Session price is **$45** —
recorded here because the ~$30 figure that appeared on an early mood board never reached
production and must not be resurrected from it.

**The image inventory is not unified, and this bug will regrow.** `js/data.js` is a SECOND
image inventory that never passes through the Eleventy transform. It is why the rotation
corrections hand-written into the templates never reached the modal galleries or the map
popups, and B7 fixed the instances rather than the class — six URLs corrected by hand, and
the next photograph added to a model will have the same problem. Post-wave unification
candidate; `models.json` is the precedent for moving a second source of truth into the
build. Until then, any URL added to `js/data.js` is sized by hand (w_1200 for modal
galleries, w_400 for map popups) and carries its own angle.

Two things ride with that unification when it happens. First, C-1 proved the class is
still live: `IMG_2891` was 90° off in `js/data.js` AND in the template, and the template
copy was passed over in B7 stream 1 on the assumption it was already right. Hand-sized,
hand-rotated URLs in two inventories cannot be kept in agreement by attention. Second,
**`w_400` is too thin for the map popups** (Razor N-7). It is a hand-picked width serving
a box that is not 400px on a 2x display, so the popup photographs are soft. It is not
worth a hand-edit now — changing it by hand is another instance of the very thing this
item says to stop doing — but the unification should let the generator choose the width
from the rendered box, as it already does everywhere the Eleventy transform reaches.

**IMG_2891's alt text describes a different photograph** — for the George/Jen copy pass.
On `/saunas/` the mosaic tile and the lightbox behind it carry
`alt="Completed sauna installation in residential backyard setting"`. The photograph is
an INTERIOR: a bather ladling water, löyly bucket, window at eye height, cedar walls. The
SUBJECT is wrong, not the wording, so this is not a tidy-in-place — a screen-reader
visitor is told about an exterior that is not in the frame. Found during fix C-1 (where
the image was also 90° off, which is fixed), and flagged rather than silently corrected:
user-facing copy belongs to George and Jen, and a rotation fix is not the place to put
new words past review.

**`models-json:selftest` leaves the tree dirty only under SIGKILL** — and the version of
this finding I filed first was wrong, which is why it is written out properly here rather
than carried forward. I reported that the suite "has no trap handler". It has all of
them: `snapshot()`/`restoreAll()`, SIGINT and SIGTERM handlers, an `uncaughtException`
handler, a `finally { restoreAll() }`, and a closing check that every mutated file came
back byte-identical. Reproduced both cases directly against the running script:

    SIGTERM   handlers run       -> mutations restored, tree clean
    SIGKILL   handlers bypassed  -> src/_data/models.json left mutated

So the hazard is narrow and is NOT fixable in-script: a harness enforcing a timeout with
SIGKILL cannot be trapped, and the run that bit us was exactly that — a 10-minute kill
while the suite sat at the end of a long `for` loop. Operationally: give this suite an
uninterrupted run of its own, and after ANY interrupted run check `git status` before
trusting the next one. A leftover mutation makes the following run fail M0 ("the shipped
tree is green before anything is broken") for a reason that has nothing to do with the
code, which is precisely how it cost a cycle here.

**The homepage video is 59MB and dwarfs everything else on the site** (Razor N-9). Its own
named post-wave item, because it is pre-existing, it is NOT this wave's to fix, and it is
large enough that leaving it inside a performance bullet would understate it by an order of
magnitude. Measured on the wire 2026-08-02, the exact URL in `home.njk:143`
(`.../video/upload/q_auto,f_auto,ac_none,w_1920/v1768415312/ambleside_beach_zqywgc.mp4`):
**59,184,462 bytes**. Recorded as measured rather than as remembered — an earlier note put
it near 32MB, and the two numbers should not be averaged into a vague one.

The element is `<video autoplay muted loop playsinline>` with no `preload` and no `poster`,
so a browser fetches the whole asset on every homepage view and then loops it. For scale:
every byte B7 saved across the entire site is roughly one percent of this file. Nothing
else on the route is in the same class.

Not fixed here and deliberately not half-fixed — the options trade against each other and
one of them is Jen's call, not an engineer's: a poster frame plus `preload="none"`;
a much shorter loop; a lower bitrate or a smaller `w_`; `ac_none` is already dropping the
audio track so that lever is spent; or dropping the video plate. The first is the obvious
floor and would cost nothing visually above the fold, but the plate is a composition
decision and shortening or removing it is a design change.

**WETT policy ruling.** All future wood-fired builds are to be designed WETT-certifiable.
This is insurer-driven, not a preference. Flagged for the pricing owner: it has
configurator and models implications that have not been costed.

## AI Advisor Widget (parked, needs more thought)

**What it is.** A chat widget that lives on contact, about, faq, saunas, and warranty pages. Context-aware starters per page (product, care, commercial, etc.), multi-turn on some pages, single-shot on others.

**Current state.** Code is in the repo but gated off via `site.features.advisor = false` in `src/_data/site.json`. Backend is `netlify/functions/advisor.js`. Frontend is `js/advisor.js`. Containers are already wired into the relevant templates — just wrapped in `{% if site.features.advisor %}`.

**Why it's off.** The backend wasn't fully wired and the widget wasn't functioning in production. Good idea, shipped too early.

**What it would need before re-enabling:**
- Backend function verified end-to-end against current Anthropic SDK
- Prompts reviewed per page type; right now they're generic and duplicate work the page copy already does
- UX decision: is a chat widget even the right surface? The contact page already has a structured form. A chat widget on top risks split-brain UX (which one do I fill in?). Consider:
  - Advisor on product/FAQ pages only (where questions are open-ended)
  - Advisor ONLY as a fallback when a form field is blank and user stalls
  - Or skip the widget entirely and use the saved prompts to improve static FAQ content
- Analytics to verify it actually drives inquiries rather than replacing them with conversations that never convert
- Rate limiting + abuse handling on the function

**Re-enabling:** flip `features.advisor` to `true` in `src/_data/site.json`. All existing templates will render their containers again and the script tag loads.
